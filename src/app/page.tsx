import Link from 'next/link';
import { ArrowRight, ShieldCheck, Phone } from 'lucide-react';
import { startDemo } from '@/server/actions';
export const dynamic = 'force-dynamic';
export default function Landing() {
  const live = process.env.APP_MODE === 'live';
  return <div className="landing"><header className="landing-nav"><Link className="brand" href="/"><span className="brand-symbol"><ShieldCheck size={23}/></span>사업장 안전관리</Link><Link className="emergency-link" href="/emergency"><Phone size={17}/>사고·긴급 연락</Link></header><main className="landing-main enterprise-landing"><section><p className="eyebrow">사업장 안전관리 업무 공간</p><h1>사업장에 맞는 업무를.<br/>준비는 하나씩.</h1><p className="landing-lede">사업장 정보를 입력하면 관련 업무를 안내합니다.<br/>필요한 내용을 확인하고, 자료를 하나씩 준비하세요.</p><div className="button-row">{live ? <Link className="button primary" href="/login">회원 로그인 <ArrowRight size={18}/></Link> : <><form action={startDemo}><button className="button primary">가상 사업장으로 체험하기 <ArrowRight size={18}/></button></form><Link className="text-link" href="/login">회원 로그인</Link></>}</div>{!live ? <p className="muted">회원가입 없이 체험합니다. 실제 고객 자료는 입력하지 않습니다.</p> : null}</section><section className="landing-preview" aria-label="이용 순서 예시"><div className="preview-head"><span className="small-label">사업장 정보에서 시작합니다</span><span>가상 화면</span></div><h2>정보를 확인하면,<br/>다음 할 일이 보입니다.</h2>{[['등록정보·실제 작업 입력','01'],['인원·고용관계 확인','02'],['관련 업무와 근거 확인','03'],['자료 준비·수행 기록','04']].map(([title,label]) => <div className="preview-task" key={title}><strong>{title}</strong><span>{label}</span></div>)}{!live ? <Link className="text-link" href="/demo">체험 범위 먼저 보기 <ArrowRight size={16}/></Link> : null}</section><p className="scope-note">{live ? '자료 작성·실제 수행·사람 검토·기관 접수는 각각 확인합니다. 법령 적용 여부와 제공 범위는 업무별 안내를 확인해주세요.' : '현재는 가상 자료와 DEMO 규칙을 사용하는 업무 흐름 체험입니다. 검토 후보 목록은 실제 법적 의무의 적용 판단이나 전체 법령 대응을 뜻하지 않습니다.'}</p></main></div>;
}
