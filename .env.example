# This release supports demo only. live intentionally fails until P2 is connected.
APP_MODE=demo
# off | mock | live (live is not connected in P1)
AI_MODE=mock
OCR_MODE=mock
# P1 requires no external credentials. Never put secrets in NEXT_PUBLIC_* variables.
