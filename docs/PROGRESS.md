# 개발 진행 기록

업데이트: 2026-09-23
현재 단계: 13개 관점의 반복 검수, 위험요인 제외 보호·날짜 오류 복구·자료 검색·저장 후 포커스 개선 구현. 최종 브라우저 회귀 15개·전체 단위 240개·빌드·lint 통과. 실제 연결·파일 검사·운영 복구·법령 승인·현장 사용자 검증은 미완료.


## 2026-09-23 · 13개 관점의 반복 검수와 입력 복구

### 검토 조직과 제품 판정

- 사용자 요청에 따라 [제품 개선 조직](PRODUCT_TEAM.md)을 13개 관점으로 구체화하고, [검토 목록](PRODUCT_REVIEW_BACKLOG.md)에 재현 근거·반론·수용 기준·상태·다음 작업을 남겼다. AGENTS.md에도 다음 작업의 반복 절차를 연결했다. [회차 설계](plans/2026-09-23-inclusive-product-review-design.md)를 기준으로 기존 승인 범위 안에서 구현했다.
- 3개 에이전트가 디자인/상호작용/구매, 초보/고령/현장/저시력, 법령/제조업/다사업장/운영 관점을 나눠 감사했다. 총괄은 제품/개발 신뢰성 관점을 맡았다. 여러 가상 관점이 실제 고객 13명이나 법률 전문가 검토를 의미하지 않는다.
- 현재는 후보 안내·기록·보완 도구이며 전 업종·규모의 법적 이행을 모두 지원하는 제품이 아니다. 법령 적용 원장, 설비별 수행건, 본사 결정과 현장 조치 연결, 시민재해 범위 확인, 일반 업무 반출이 남아 있다. 승인 규칙 0개·미연결 조문 73개는 유지하며 법적 의무 전체 수로 해석하지 않는다.
- 공식 법령 원문과 W3C 접근성 안내를 읽어 감사 근거를 기록했다. 법령 규칙·조문·수치·시행일을 새로 승인하거나 변경하지 않았다. 06-01 조문 개정일/08-01 전체 시행본은 오기로 단정하지 않고 원문 식별 재확인 항목으로 남겼다.

### 구현한 흐름과 2차 검수

- 위험요인: 빈 새 항목은 즉시 취소하고 내용 있는 항목은 대상·제외 범위·이전 저장본 보존을 설명한다. 계속 작성을 기본 포커스로 두고 Esc/취소 시 값·ID·현재 위치를 보존한다. 확정은 이번 초안의 미저장 변경이며 위험 해소가 아니다.
- 날짜 오류: 서버 검증을 유지하면서 실제 오류 필드와 유일한 위험요인 ID를 반환한다. 숨은 단계·펼침·위험요인으로 이동해 입력에 초점을 두고 이유를 연결한다. 다른 입력·미확인 값·기존 저장 기준을 유지한다. 미래 계획일은 허용하며 미래 실제일/역전/잘못된 형식은 저장하지 않는다.
- 자료함: 제목 검색을 먼저 두고 현장·상태는 검색 조건으로 펼친다. 활성 조건은 자동 펼치고 조건 요약·결과수·초기화·정확한 과거 검토 링크를 보존한다. 모바일 첫 화면에 자료 제목을 노출한다.
- 저장 안내: 상세 성공 안내가 있을 때 중복 일반 성공 띠를 숨긴다. 저장/실패 이벤트는 저장 중 입력 잠금이 풀린 뒤 전달하여 다음 질문이나 오류 입력에 초점을 둘 수 있게 했다. 저장 중·실패·미저장 안내와 정확한 savedBasis/revision 처리는 보존한다.
- 구현자와 다른 담당이 재검수했다. 관련 시작일을 고쳐 저장하면 이전 종료일 오류가 남는 P2를 발견해 성공 시 오류를 지웠다. 검사 라벨 불일치와 기존 취소 검사에 새 확인창 조작 누락도 수정했다. 서버 권한 선확인·실패 시 쓰기 없음·유일한 ID, 자료 검색 URL·조건·상세 링크, 저장 이벤트의 중복/지난 성공 오인 여부를 읽기 검수했다.

### 변경 파일

- 지침/문서: AGENTS.md, docs/{PRODUCT_TEAM,PRODUCT_REVIEW_BACKLOG,DECISIONS,PROGRESS}.md, docs/plans/2026-09-23-inclusive-product-review-design.md.
- UI: src/components/{risk-hazard-remove,risk-assessment-form,workspace-form,document-list}.tsx, src/app/readability.css.
- 서버/도메인: src/domain/{risk-assessment,workspace}.ts, src/server/task-actions.ts. DB·RPC·법령 규칙·의존성 변경 없음.
- 검사: tests/unit/{risk-assessment,task-save-actions}.test.ts, tests/e2e/{risk-input-recovery,persona-recovery,persona-library,persona-feedback,hazard-resume,document-search}.spec.ts. 기존 미커밋 변경과 무관한 debug.log는 보존했다.

### 실행·검증

- 기존 3021 데모에서 독립 감사 스크립트 2개 실행: 위험요인 1→0 무확인 취소, 인원 관계 오류의 숨은 필드, 내용 확인 후 증빙 경로 단절을 재현했다. 360/1440px 자료함·홈·등록·위험성평가 캡처에서 검색 위계와 저장 후 입력 하단 가림을 확인했다. 실제 고객·휴대전화·장갑 시험은 아니다.
- npm.cmd test -- tests/unit/risk-assessment.test.ts tests/unit/task-save-actions.test.ts --maxWorkers=2: 2파일·36개 통과. npm.cmd test -- --maxWorkers=2: 전체 25파일·240개 통과(42.64초). 실제 Supabase 호출은 mock이며 실연결 검증이 아니다.
- npm.cmd run lint 전체 통과. npm.cmd run build 프로덕션 빌드·타입 검사 통과. 첫 브라우저 검사 후 성공 이벤트 시점 변경을 반영하기 위해 중간 빌드 1회는 완료 전 중단했으며 최종 전체 lint/build를 다시 통과했다.
- PLAYWRIGHT_PORT=3023, npm.cmd run test:e2e -- tests/e2e/{risk-input-recovery,persona-recovery,persona-library,persona-feedback}.spec.ts --output=test-results/persona-round1: **4개 통과·1개 실패(7.9분)**. 실제 명령은 네 파일을 각각 지정했다. 실패는 persona-feedback의 다음 질문 포커스였으며 입력 가림 판정까지 도달하지 못했다. 가림은 별도 감사 캡처에서 재현된 문제다. 실패를 검사 라벨 문제로 돌리거나 기준을 낮추지 않고 입력 잠금 해제 뒤 성공 이벤트를 전달하도록 수정했다.
- 최종 명령: PLAYWRIGHT_PORT=3023, npm.cmd run test:e2e -- tests/e2e/persona-feedback.spec.ts tests/e2e/persona-library.spec.ts tests/e2e/persona-recovery.spec.ts tests/e2e/risk-input-recovery.spec.ts tests/e2e/save-revision-refresh.spec.ts tests/e2e/hazard-resume.spec.ts tests/e2e/document-search.spec.ts tests/e2e/save-state.spec.ts tests/e2e/contextual-guidance.spec.ts --grep "모바일 저장 후|자료함 첫|위험요인 제외|숨은 두 번째|접힌 교육|서버 갱신도|자신의 연속|사업장 정보도|두 번째 위험요인의|보던 위험요인을|자료함에서|사업장 단계 이동|위험요인 추가·삭제|도움 요청과 실제 저장" --output=test-results/persona-final: **15개 모두 통과(11.2분)**. 첫 실패 시나리오도 통과했으며 최초 실패 기록을 보존한다. 반복 실행을 합산하지 않은 서로 다른 15개 시나리오다.
- 새 검사: 360/1440px 자료 검색의 첫 제목·실제 클릭 가림·키보드·조건/재열람, 위험요인 제외 계속 작성/Esc/명시 확정·입력 ID/과거 v1 보존, 두 번째 위험요인의 숨은 오류·접힌 교육 날짜·역전된 평가 기간·관련 날짜로 오류 해결·수정 후 재열람, 저장 후 다음 질문 포커스·입력 상하단 클릭 대상·미저장 안내를 확인했다. 오류와 제외 확인 화면의 axe 및 모션 감소도 확인했다.
- 회귀: 기존 저장 안내/도움/홈 재개, 두 번째 위험요인의 위치 재개·전송 실패·제외 후 위치 복구, 자료 검색, 실제 Next router.refresh 뒤 동시 수정 방어, 연속 저장·증빙·내용 확인 뒤 보완, 등록·위험요인 임시 저장/원상복원/실패/이탈 보호를 통과했다. DB/RPC 변경이 없어 DB 전용 검사는 이번에 재실행하지 않았다.
- 화면 산출물: test-results/persona-feedback-360.png, persona-library-{360,1440}.png, persona-exclude-360.png, persona-date-error-360.png. 수정된 저장 후 화면과 자료함을 육안 확인했다. 첫 가림 감사 캡처 persona-visual-risk-criteria-natural-360.png 및 실패 trace는 보존한다.
- git diff --check 통과. UTF-8 문서 6개와 상대 링크 25개 확인. 기존 .git 변경과 debug.log를 덮거나 제거하지 않았다.

### 미검증·다음 작업

- 저장 응답 유실 뒤 서버 결과와 현재 초안 대조(SAVE-01), 사업장 인원 관계 오류의 정확한 입력 복구(UX-03), 내용 확인 후 증빙 보완 경로(UX-04), 사진 관찰의 조치 추적·일반 업무 반출을 우선순위에 유지한다.
- 실제 Supabase Auth/MFA/Storage/파일 검사/권한 철회/DB와 원본 복구, 법령 전문 승인, 실제 초보·고령·시설관리 고객·실제 모바일/확대 조작 시험은 미완료다. 브라우저 모사·axe·단위 검사로 이 조건을 통과시키지 않는다.
- 공개 배포·고객 초대·외부 발송·실자료 입력은 이번 회차에 실행하지 않았다. 사용자 후속 Git 푸시 요청에 따라 검증된 앱·문서를 커밋·origin/main 반영 대상으로 확정했다. 로컬 debug.log와 테스트 산출물은 제외한다. 최종 미리보기 **http://127.0.0.1:3021/demo**를 재시작했다. APP_MODE=demo, AI_MODE=mock, OCR_MODE=mock, SAFETY_ENABLE_TEST_FIXTURES=0, npm.cmd run start -- --hostname 127.0.0.1 --port 3021. GET /demo 200, POST /api/testing/session 404 확인. 실제 고객 원본의 보존 환경이 아닌 데모다.

## 2026-09-23 · 답변과 실제 저장 결과에 반응하는 UX

### 구현한 흐름

- 사용자의 정정대로 색·글자 정리에 그치지 않고 [답변별 안내 계획](plans/2026-09-23-contextual-guidance-design.md)을 구현했다. 디자인 담당이 초보자의 단절 지점을 감사하고, 개발/디자인 담당이 분리 구현했으며 운영 담당이 상태·문구를 읽기 검수했다. 실제 고객 시험은 아니다.
- 사업장 정보의 도급 역할에 따라 관련 설명과 다른 업체 인원 입력이 선택 바로 아래에 나타난다. 역할 전환으로 기존 숫자를 지우지 않고 미확인과 0명을 구분한다. 확인 화면의 필수 누락·미확인 버튼은 정확한 단계/입력으로 이동하고 필요한 펼침을 연다.
- 위험성평가 시작은 작업·장소 질문에 집중하고 구분·담당·일정은 펼쳐 입력한다. 전체 직접 입력에서도 기존 필드에 접근한다. 판단 기준은 긴 근거 설명보다 먼저 입력한다.
- 설명·교육이 필요한 답변에 방법 안내·공식 자료를 연결했다. 도움말 열람은 교육 실시나 참석 사실을 만들지 않는다. ‘확인 필요로 저장’은 그 상태로 보관 후 다음 질문으로 이동하며, 임시 저장은 현재 위치를 유지한다.
- 실제 성공 이벤트 뒤에 보관한 질문과 다음 질문을 표시한다. 편집·삭제·수동 이동·재제출 시작 시 지난 안내를 지우고 오류/저장 중에는 표시하지 않는다. 짧은 안내 등장·질문 전환·체크 모션과 모션 감소를 적용한다.
- 홈은 저장 버전이 있는 현재 위험성평가 일반 초안에 한해 ‘보던 질문’과 질문별 재개 버튼을 표시한다. 기존 권한 경로의 커서를 사용하며 개선·보완·기한 우선순위와 링크는 유지한다. 보던 위치를 내용 저장이나 업무 완료로 표시하지 않는다.
- 운영 검수의 저장 안내 잔존/이동 문구 지적을 반영했다. 직접 입력 모드에서도 실제 노출되는 버튼과 도움 문구가 일치하도록 보완했다.

### 변경 파일

- UI: src/components/{business-profile,risk-assessment-form,journey-home,task-home}.tsx, src/app/readability.css.
- 신규 브라우저 검사: tests/e2e/contextual-guidance.spec.ts. tests/e2e/next-actions.spec.ts에 홈 주 안내의 개선조치 우선 보존 검사를 추가했다. 문서: docs/{DESIGN,DECISIONS,PROGRESS}.md, docs/plans/2026-09-23-contextual-guidance-design.md.
- 기존 미커밋 변경을 보존했다. 저장 컨트롤러·서버 액션·DB/RPC·도메인 규칙·의존성 변경 없음.

### 실행·검증

- 기존 미리보기 프로세스를 중단한 뒤 빌드했다. npm.cmd run lint 전체 통과. npm.cmd test -- --maxWorkers=2: 25개 파일·219개 테스트 모두 통과(33.78초). npm.cmd run build: 프로덕션 빌드와 TypeScript 통과. 문구 후속 수정 뒤에도 빌드/전체 lint를 확인했다.
- PLAYWRIGHT_PORT=3023, npm.cmd run test:e2e -- tests/e2e/contextual-guidance.spec.ts tests/e2e/journey.spec.ts tests/e2e/readability.spec.ts --output=test-results/contextual-guidance: **7개 통과, 1개 실패(4.2분)**. 새로운 역할 검사에서 select의 label 텍스트를 exact로 조회해 요소를 찾지 못하고 90초 제한에 걸렸다. 브라우저 접근성 스냅샷의 combobox 이름을 사용하도록 검사 조회를 바로잡았다. 앱 동작 검증을 삭제하거나 기준을 완화하지 않았다.
- 위 실행에서 누락 입력 7곳의 포커스/펼침/이동만으로 저장되지 않음, 도움 요청/실패/성공/홈 재개, 기존 지도·저장/위치 재개, 모바일 입력 가림/도움말 복귀, 글꼴 실패/확대/접근성/모션 감소 검사는 통과했다.
- 후속 명령: PLAYWRIGHT_PORT=3023, npm.cmd run test:e2e -- tests/e2e/contextual-guidance.spec.ts tests/e2e/business-profile.spec.ts tests/e2e/save-state.spec.ts tests/e2e/save-revision-refresh.spec.ts tests/e2e/next-actions.spec.ts tests/e2e/risk-assessment.spec.ts --grep "역할에 따른 후속|사업장 입력 네 단계|홈의 미확인 인원|저장한 개선조치는|서버 갱신도|자신의 연속 저장|사업장 정보도|위험성평가 단계 이동|위험성평가 모든 단계|사업장 단계 이동|위험요인 추가·삭제" --output=test-results/contextual-regression: **12개 모두 통과(9.4분)**.
- 후속 실행은 역할 3종/역할 변경 값 보존/0명·미확인 재열람, 등록 4단계 320·360·768·1440px, 기존 정확한 보완 위치, 홈 개선조치 우선순위/검토 후에도 남은 조치, 위험성평가 7단계/여러 위험/과거 버전/미래 수행일 차단, 다른 탭 갱신·동시 수정/자기 연속 저장/증빙 후 보완/미저장 이탈 보호를 확인했다. 첫 실패 검사도 통과했으며 최초 실패 기록은 보존한다.
- 역할 간 변경에서 설명 문구만 모션용으로 다시 표시하고 숫자 입력 DOM은 유지하도록 마지막으로 보완했다. 이 변경 뒤 npm.cmd run build(타입 검사 포함)와 수정한 컴포넌트/검사 파일의 ESLint를 다시 통과했다. PLAYWRIGHT_PORT=3023, npm.cmd run test:e2e -- tests/e2e/contextual-guidance.spec.ts --grep "역할에 따른 후속" --output=test-results/contextual-final: 최종 1개 통과(22.3초). 서로 다른 19개 브라우저 시나리오의 통과를 확인했으며 반복 실행은 별도 집계한다. 서버/DB를 바꾸지 않아 DB 전용 검사는 재실행하지 않았다.
- 캡처: test-results/contextual-profile-360.png, contextual-help-360.png와 기존 readability 캡처. 360px 후속 질문·도움·저장 버튼을 육안 확인했다. git diff --check 및 신규 파일 UTF-8/끝 공백 검사를 통과했다.

### 미검증·다음 작업

- 최종 미리보기 **http://127.0.0.1:3021/demo**를 재시작했다. APP_MODE=demo, AI_MODE=mock, OCR_MODE=mock, SAFETY_ENABLE_TEST_FIXTURES=0으로 npm.cmd run start -- --port 3021 실행. GET /demo HTTP 200, POST /api/testing/session HTTP 404를 확인했다. 테스트 서버 3023은 종료했다.
- 실제 Supabase 인증·DB/Storage·파일 검사·백업 복구·실제 iOS/Android 키보드·시설관리 고객 시험은 미실행이다. 승인된 운영 법령 규칙 0개·미연결 조문 73개와 저장 응답 유실 복구 등 기존 미완료를 유지한다.
- 다음: 시설관리 사용자가 역할 답변 → 첫 기록 → 도움 요청 → 저장 → 재개를 수행하며 다음 행동을 이해하는지 확인한다. 이후 현장 발견 한 건의 사진·조치·재확인 흐름에도 같은 안내 방식을 적용한다.
- 공개 배포·외부 발송·유료 호출·Git push 없음. 가상 체험은 메모리 저장이며 실제 고객 원본 입력용이 아니다.

## 2026-09-22 · 디자인·사용성 중심 재구축

### 구현한 흐름

- 사용자 재구축 승인과 기존 선택(모바일 현장 우선, 단계 안내 + 전체 지도, 시설관리 우선)을 적용했다. [구현 계획](plans/2026-09-22-mobile-workspace-redesign.md)에 시각·내용·모션·검증 기준을 기록했다.
- 홈은 오늘 할 일 → 다음 행동 하나 → 남은 확인 → 전체 지도 → 저장한 기록으로 정리했다. 큰 벤토·반복 그림을 제거하고 사업장 요약·전체 업무 검색·이전 기록 접근을 펼침으로 보존했다.
- 등록은 4단계의 짧은 진행 표시와 실제 질문·입력 중심이다. 등록증 세부정보·인원 집계 근거를 펼쳐 입력하며 미확인·0명·확인 체크·수정 위치 이동·저장 기준을 보존했다.
- 위험성평가는 입력을 업무 안내·회차 이력보다 먼저 배치했다. 7단계·세부 질문·전체 직접 입력·위험요인 안정 ID·입력 41개·미해결 위험·저장/재개를 유지했다. 담당자·준비 일정·추가 메모는 입력 다음에 펼친다. 그림/수동 3컷 설명은 도움말에서 제공한다.
- Pretendard와 기존 아이콘을 유지하고 흰 면·회색 구획·파란 주 행동·글자 크기·간격을 통일했다. 모바일 편집 화면은 하단 주 메뉴를 숨기고 주 저장 버튼만 고정한다. 보조 이동·설명은 본문에 두고 도움말을 화면 폭에 맞는 하단 패널로 표시한다. 모션 감소와 포커스 복귀를 보존한다.
- 실제 연결용 회원 셸도 공통 탐색을 사용하도록 정리했다. 로그인·서버·도메인·DB·권한·revision 로직은 변경하지 않았다. Supabase가 없어 실제 로그인 이후 탐색은 미검증이다.
- 홈/등록/업무 입력을 독립 담당자가 구현하고 교차 검수했다. 회차 안내의 잘못된 방향 문구를 바로잡았다. 에이전트 검수는 실제 고객·법률 전문가 검증과 다르다.

### 변경 파일

- UI: src/app/readability.css, src/app/workspace/layout.tsx, src/components/{shell,navigation,journey-home,journey-welcome,task-home,business-profile,risk-assessment-form,risk-story,task-detail,task-forms,task-guidance,task-cycle-history}.tsx.
- 브라우저 검사: tests/e2e/{profile.ts,business-profile.spec.ts,readability.spec.ts,risk-assessment.spec.ts,tasks.spec.ts}. 제목/펼침 경로 변경에 맞게 기존 검증을 유지하고 첫 화면 입력·버튼 위치·가림·하단 도움말 검사를 추가했다.
- 문서: docs/{DESIGN,DECISIONS,PROGRESS}.md, docs/plans/2026-09-22-mobile-workspace-redesign.md. 의존성·비밀값·DB 마이그레이션 변경 없음.

### 실행·검증

- npm.cmd run lint: 최종 전체 검사 통과. 입력 순서·저장 영역 후속 수정까지 재검사했다.
- npm.cmd test: 첫 실행은 81개 통과 후 15개 worker 시작 시간 초과로 실패했다. npm.cmd test -- --maxWorkers=2 재실행은 25개 파일·219개 테스트 모두 통과했다.
- npm.cmd run build: 화면·저장 영역 수정 후 최종 프로덕션 빌드·타입 검사 통과.
- PLAYWRIGHT_PORT=3023 npm.cmd run test:e2e -- tests/e2e/readability.spec.ts tests/e2e/business-profile.spec.ts tests/e2e/journey.spec.ts tests/e2e/mobile-map.spec.ts tests/e2e/save-state.spec.ts tests/e2e/save-revision-refresh.spec.ts tests/e2e/next-actions.spec.ts tests/e2e/risk-assessment.spec.ts tests/e2e/tasks.spec.ts tests/e2e/task-cycles.spec.ts: 30개 중 27개 통과, 3개 실패(20.2분). 실패를 성공으로 집계하지 않는다.
- 첫 화면 검사는 위험성평가 입력 하단이 800px 화면에서 832.7px에 놓인 문제를 잡았다. 모바일의 숨긴 사이드바에 남은 PC용 60px 여백을 제거하고 입력 부가 설명을 아래로 옮겼다. 회차 검사에서 v1 표시의 대비 4.49:1을 발견해 배경·글자색·크기를 수정했다. 7단계×3화면/접근성 복합 검사는 90초 제한에 걸려 개별 검사 한도를 180초로 조정했다.
- 새 빌드로 가독성·위험성평가·회차·지도·등록 화면 9개를 재검증: 8개 통과, 도움말 애니메이션 중 위치를 측정한 검사 1개 실패(6.6분). 실제 7단계·미래 수행일 차단과 회차 이력·대비 검사는 통과했다. 도움말 위치를 애니메이션 종료 시점까지 확인하도록 보정하고 브라우저 기본 최대 폭도 해제해 모바일 전체 폭으로 표시했다.
- 다음 가독성 3개 실행은 2개 통과, 입력 가림 검사 1개 실패(1.8분). 고정 영역에 보조 이동·설명까지 들어가 위험성평가 첫 입력을 덮는 실제 결함을 발견했다. 등록·위험성평가·공통 기록의 고정 영역에는 주 저장 버튼만 남겼다. 범위·임계값을 완화하지 않고 입력 상단/하단의 실제 클릭 대상과 입력 후 가림까지 확인한다.
- 최종: PLAYWRIGHT_PORT=3023 npm.cmd run test:e2e -- tests/e2e/readability.spec.ts --output=test-results/redesign-savebar → 3개 모두 통과(31.1초). 홈 첫 행동의 화면 내 위치, 첫 입력 가림 없음, 전체 폭 도움말·복귀, 입력 보존·저장·새로고침 재개, 글꼴 실패·확대·모션 감소를 확인했다.
- 최종 저장 영역 회귀: PLAYWRIGHT_PORT=3023 npm.cmd run test:e2e -- tests/e2e/business-profile.spec.ts tests/e2e/save-state.spec.ts tests/e2e/tasks.spec.ts --grep "사업장 입력 네 단계|사업장 단계 이동|위험요인 추가·삭제|목록 → 전용" --output=test-results/redesign-save-regression → 4개 모두 통과(1.4분). 등록 4단계 320/360/768/1440px·접근성, 임시 저장/실패/재열람, 위험요인 추가·삭제/저장 중/이탈 보호, 증빙·별도 검토자 보완·새 버전 흐름을 확인했다. 최초 실패 3개는 후속 실행에서 모두 통과했으며 중간 실패 기록은 보존한다.
- 320/360/768/1440px 등록 4단계, 홈 360/1440px·200% 상당 확대·글꼴 실패, 도움말 키보드/모션 감소, 지도/상세 수치, 실패 후 입력 보존, 다른 탭 갱신·동시 수정·연속 저장·검토 요청 보호, 별도 검토자 보완, 미확인/0 구분은 첫 실행에서 통과했다. 실제 모바일 기기·브라우저 확대 UI 조작·접근성 인증으로 해석하지 않는다.
- 캡처는 test-results/에 생성하고 홈·등록의 모바일/데스크톱, 도움말, 최종 위험성평가 첫 화면을 육안 확인했다. 최종 대표 화면은 readability-home-360.png, readability-home-1440.png, redesign-profile-360.png, redesign-risk-first-360.png, redesign-risk-360.png다. git diff --check 통과.

### 실행 범위·다음 작업

- 로컬 체험은 APP_MODE=demo, AI_MODE=mock, OCR_MODE=mock, SAFETY_ENABLE_TEST_FIXTURES=0로 실행한다. 최종 미리보기 http://127.0.0.1:3021/demo 를 갱신하고 HTTP 200을 확인했다. 테스트 세션 API는 HTTP 404로 차단됨을 확인했다. 체험 자료는 메모리 저장이므로 재시작 시 초기화된다.
- 실제 Supabase 인증·DB/Storage·파일 검사·백업 복구·실제 iOS/Android 키보드·시설관리 고객 사용성 시험은 미실행이다. 승인된 운영 법령 규칙 0개·미연결 조문 73개를 이번 UI 완료로 해소했다고 표시하지 않는다. 기존 미완료 항목을 보존한다.
- 다음: 실제 시설관리 사용자에게 등록 → 첫 현장 기록 → 다시 열기 → 보완 과제를 수행하게 해 이해/실패 지점을 검증한다. 이어서 저장 응답 유실 확인, 현장 발견 한 건의 조치·재확인, 실제 연결과 법령 승인 작업을 진행한다.
- 공개 배포·외부 발송·JEV 연동·추가 의존성 설치·Git push는 이번 단계에서 하지 않았다.

## 2026-09-21 · Jev Ultrafast 적용 검토

- 사용자 지정 저장소 browser-use/jev-ultrafast를 대상으로 디자인·고객, 개발·보안, 운영 관점에서 문서·코드를 읽고 반론을 대조했다. [도입 검토안](plans/2026-09-21-jev-adoption-review.md)에 구체적 적용 후보·소스 근거·실험 조건을 기록했다. 실제 고객·법률 전문가 검토는 아니다.
- 권고: 전체 브라우저 에이전트는 격리된 합성 환경의 탐색 검수부터 비교한다. 고객 안내는 기존 결정적 다음 행동을 유지하며, 긴 메모의 확인 질문 후보는 Jev 모델만 별도 평가할 수 있다. 공식 자료 수집은 API·원문 경로를 우선하고 UI 예외 탐색만 후보로 둔다.
- 기본 데스크톱 실행, 파일·프레임·일부 날짜 입력 제약, 자동 제출 지침, confidence와 실행 통제의 차이, DONE의 독립 검증 필요를 확인했다. 공개 소스의 정적 검토이며 실제 실행 재현 결과가 아니다.
- 모델로 화면 텍스트·입력값이 전달되는 구조와 우리 MFA 등록 키·초대 코드가 일반 읽기 전용 입력에 표시되는 접점을 확인했다. 실제 비밀값을 읽거나 전송하지 않았다. 고객·운영 계정 프로필과 민감 화면을 연결하는 방안은 권고하지 않는다.
- 변경 파일: docs/plans/2026-09-21-jev-adoption-review.md, docs/PROGRESS.md. 앱 코드·의존성·환경·DB 변경 없음. 기존 저장 신뢰성 → 현장 발견 한 건의 조치·재확인 → 교육 한 종류 순서를 유지한다.
- 실행: Get-Location/git rev-parse/git status로 작업 위치·기존 변경 확인, rg/Get-Content로 관련 코드·제품 문서 대조, 공식 GitHub·TypeSafe 문서 조회. JEV 설치·모델 API·유료 호출·계정 연결·고객 원본 전송·공개 배포는 하지 않았다.
- 문서 검증: 변경 문서 2개의 UTF-8·로컬 링크 20개와 신규 문서의 끝 공백 검사 통과. git -c core.safecrlf=false diff --check -- docs/PROGRESS.md 통과(Windows 줄바꿈 안내만 출력).
- 미검증: 한국어 분류/탐색 성공률·국내 지연·전체 비용·실제 모바일·공급자 개별 계약 적합성. 앱을 변경하지 않아 빌드/단위/DB/E2E를 재실행하지 않았고 과거 통과를 JEV 연동 검증으로 집계하지 않는다. 실행 명령과 수용 조건은 검토 문서의 제안 범위이며 아직 실행 결과가 아니다.
- 다음: 저장 응답 유실의 결과 확인과 현장 기록 개선을 계속한다. JEV를 구현할 경우 별도 가상 환경·호출 권한/예산·독립 판정 기준을 준비한 뒤 작은 탐색 실험부터 수행한다.

## 2026-09-21 · 다음 문제 토론과 저장 기준 보강

### 토론 결과와 다음 흐름

- 사용자 “계속 토론해서 발전시켜봐 다음문제는 뭐야” 요청에 따라 현재 코드로 디자인·개발/보안·운영 교차 검토와 반론을 수행했다. 실제 고객 인터뷰나 외부 법률 검토는 아니다.
- [다음 개선안](plans/2026-09-21-field-observation-review.md)에 일반 현장 기록의 후속 조치가 자유문장에 머무는 문제, 새 발견과 기존 보완의 구분 부재, 교육 등의 추상적 입력을 근거와 함께 정리했다. 설명이 없거나 과거 버전이 삭제되는 문제로 과장하지 않는다.
- 우선순위: 저장 신뢰성 보강 → 시설관리 현장 발견 한 건의 기록·조치·재확인 → 교육 한 종류의 전용 흐름. 모든 업무 회차 일괄 확대와 위험성평가 구조로의 자동 승격은 제외한다.
- 현장 기록 기능은 이번 토론에서 구현 완료하지 않았다. 독립 기록 ID·연결 문서·명시적 조치/재확인·권한 적용 요약과 기존 원문 보존을 다음 구현 범위로 정했다.

### 발견한 저장 경로와 현재 실행

- 개발 정적 감사에서 깨끗한 입력 화면에 다른 탭의 저장 이후 RSC 새로고침이 적용될 때, 최초 본문과 최신 hidden revision이 섞일 수 있는 경로를 찾았다. 기존 빌드의 3023 포트에서 신규 브라우저 시험을 실행해 재현했다. 오래된 작업 범위가 화면에 남았는데 저장 결과가 error가 아닌 saved로 반환돼 보호 검사가 실패했다. 위험성평가·공통 업무와 사업장 입력에 본문/입력 메타데이터와 기준 revision 묶음을 연결했다. 자기 저장 성공 응답만 정확한 저장 기준으로 받아들이고, 동일 본문의 확인·증빙 메타 갱신만 허용한다. 다른 탭의 본문 변경은 입력을 보존한 채 충돌 처리하도록 수정했으며 새 브라우저 재현과 기존 안내·저장·회차 회귀 12개를 통과했다.
- 별도로 서버 저장 후 응답만 유실될 때 기존 revision 재시도가 충돌하는 경로를 찾았다. 아직 실제 응답 유실 시험과 복구 기능 구현은 하지 않았다. 이미 저장됨·미저장·다른 수정 있음의 구분이 다음 저장 신뢰성 과제다.
- 실제 Supabase 환경 없음, 파일 검사/복구 미검증, 승인 법령 규칙·실제 고객 검증 등의 기존 미완료 조건은 유지한다.

### 변경 파일과 실제 검증

- 앱 5개: src/components/workspace-form.tsx, task-forms.tsx, business-profile.tsx, src/domain/workspace.ts, src/server/task-actions.ts. SQL과 위험성평가 입력 컴포넌트 자체는 변경하지 않았다. 입력을 자동 재초기화하지 않고 기준을 결합해 충돌 시 현재 입력을 보존한다.
- 테스트 3개: tests/e2e/save-revision-refresh.spec.ts 신규, tests/unit/task-save-actions.test.ts 신규, tests/unit/profile-actions.test.ts 보강. 계획·DECISIONS·이 진행 기록을 갱신했다.
- 수정 전 재현: PLAYWRIGHT_PORT=3023, npm.cmd run test:e2e -- tests/e2e/save-revision-refresh.spec.ts --workers=1 --output=test-results/revision-before → 깨끗한 화면의 RSC 갱신 후 오래된 입력 저장을 허용해 보호 검사 실패(Expected error / Received saved).
- 수정 후: PLAYWRIGHT_PORT=3023, npm.cmd run test:e2e -- tests/e2e/save-revision-refresh.spec.ts tests/e2e/save-state.spec.ts tests/e2e/journey.spec.ts tests/e2e/task-cycles.spec.ts --workers=1 --output=test-results/revision-after → **12개 모두 통과(5.1분)**. 실제 Next router.refresh로 검증했으며 재현을 숨길 수 있는 전체 페이지 reload로 대체하지 않았다. 테스트 서버 3023은 종료했다.
- 신규 4개는 깨끗한 입력/편집 중 입력의 외부 갱신 보호, 자기 연속 저장과 증빙·내용 확인 후 보완, 사업장 정보 충돌이다. 기존 저장·이탈·보완·안내·새 회차/과거 기록 보존 8개도 통과했다.
- 프로덕션 빌드(TypeScript 포함)와 선택 ESLint 8개 파일 통과. npm.cmd test -- --maxWorkers=1 → 최종 25개 파일 219개 통과(30.11초). npm.cmd run lint 전체 통과. DB 스키마와 RPC는 바꾸지 않아 이번에 DB 전용 검사는 재실행하지 않았다. 이전 73개 통과를 이번 수정의 새 실행 결과로 집계하지 않는다.
- 저장 응답 유실의 결과 조회·중복 재시도 복구, 실제 모드의 초기 업무/문서/본문 조회를 단일 DB 스냅샷으로 묶는 작업은 미완료다. 이번 결과는 클라이언트 부분 새로고침으로 생기는 입력/기준 혼합을 재현하고 방지한 범위다.

- 변경 문서 3개의 UTF-8·로컬 링크 21개 확인과 git -c core.safecrlf=false diff --check를 통과했다.

### 실행 방법과 다음 작업

- 최종 빌드의 **http://127.0.0.1:3021/demo** 미리보기를 재시작했다. APP_MODE=demo, AI_MODE=mock, OCR_MODE=mock, SAFETY_ENABLE_TEST_FIXTURES=0으로 npm.cmd run start -- --port 3021 실행. /demo HTTP 200, 시험 역할 전환 /api/testing/session POST 404를 확인했다. 체험은 가상 자료·메모리 저장이며 실제 고객 원본 입력용이 아니다.
- 바로 다음은 저장 응답 유실의 처리 결과 확인과 동시 조회 일관성이다. 그다음 시설관리 현장 기록별 조치·재확인 흐름, 이후 교육 한 종류의 전용 입력으로 진행한다. [다음 개선안](plans/2026-09-21-field-observation-review.md)에 사용자 과제와 수용 기준을 기록했다.

## 2026-09-21 · 제품 조직 구성과 반복 운영 개선

### 구현한 사용자 흐름

- 사용자 답변 확정: 단계 안내 + 전체 지도, 모바일 현장 사용 우선, 시설관리 사업장부터 검증 후 확대. Supabase 개발 환경은 아직 없으므로 연결 준비와 로컬 검증을 진행한다. 제품·디자인·조직·결정·실행 계획에 반영했다.

- 사용자의 지속 개선 요청으로 [제품 개선 조직](PRODUCT_TEAM.md)과 [실행 계획](plans/2026-09-21-repeat-operations-implementation.md)을 구성했다. 세 담당 에이전트와 제품 책임자가 초보 사업주·작업자·구매자 관점을 함께 검토했다. 실제 고객 인터뷰나 전문가 승인은 아니다. 기존 변경과 과거 미완료 항목을 보존했으며 외부 발송·공개 배포·커밋은 하지 않았다.
- 위험성평가 회차와 문서 수정 버전을 분리했다. 새 회차는 빈 기록으로 시작하며 이전 문서·사진·수행·검토는 보존한다. 홈과 지도는 현재 회차를 안내하고 지난 미해결·미확인·보완 사항도 남긴다. 내보내기에도 회차와 버전을 함께 표시한다.
- 여러 위험요인 중 마지막에 보던 항목을 안정 ID로 복원한다. 삭제되거나 미저장된 항목은 다른 항목으로 자동 이동하지 않고 다시 선택하도록 안내한다.
- 자료·검토함은 현장·상태·제목 검색과 25건 페이지 이동을 제공한다. 권한 적용 후 집계하며 실제 요청 시각으로 검토 대기를 정렬한다. 체험 자료함에도 검색을 연결하고 없는 요청일은 만들어 넣지 않는다.
- 업로드 호출 예외와 완료 응답 유실을 처리한다. 실패·사용 제한·10분 경과 업로드 대기는 저장 본문만 같은 문서의 새 수정 버전으로 만들어 파일을 다시 연결한다. 이전 파일·검사 이력은 보존하고 검사 대기를 통과 처리하지 않는다.
- JSONB로 변환할 수 없는 본문 하나가 홈 전체 요약을 중단하던 결함을 오류 격리로 수정했다. 해당 기록은 집계 확인 필요로 남긴다.

- 모바일 지도에서 분야를 골라도 상세가 화면 밖에 남는 탐색 결함을 수정했다. 단일 열 화면은 선택한 제목으로 포커스·스크롤을 연결하고 모션 감소를 준수한다. 넓은 화면은 기존 동작을 유지한다.

### 교차 검수와 수정

- 1차: 회차 부재, 여러 위험요인의 재개 불일치, 자료 100건 제한을 코드에서 확인해 우선 구현했다.
- 2차: 과거 검토 완료 기록의 미판단 위험이 홈에서 숨는 문제, 과거 회차 주요 행동의 저장 메타데이터 누락, 긴 회차 이력, 보완 요청의 모호한 이유를 고쳤다. 새 버전 링크·일반 업무 회차 표시·파일 대기 안내도 수정했다.
- 실제 브라우저에서 새 회차가 생성돼도 폼이 먼저 사라져 이동 결과를 처리하지 못하는 결함을 찾았다. 결과를 처리할 때까지 폼을 유지하도록 수정했다. 위험요인 테스트 두 건은 선택상자 검색 방식 오류였고 접근성 역할로 찾도록 수정했다.

### 변경 범위

- 서버·도메인: task cycle 정의/동작/현재회차 선택, 안내 위치, 자료 검색, 파일 업로드·복구, 위험성평가 출력.
- UI: task-cycle-history, task-detail/home, journey-home/map, task-board, document-list, 회원/운영자 자료·검토 화면, evidence-upload/retry와 파일 목록 새로고침, document-panel.
- DB: 신규 0010 회차, 0011 위험요인 커서, 0012 자료 검색, 0013 파서 오류 격리, 0014 파일 재시도.
- 검증: 회차·재개·검색·파일 복구 단위/DB 테스트와 새 브라우저 흐름. 제품 조직·실행 계획·결정·P2 설정·현재 진행 기록.

### 실제 검증

- 전체 단위 검사: npm.cmd test -- --maxWorkers=1 → 최종 24개 파일 212개 통과.
- 1차 선택 브라우저 검사 5개는 기존 안내 2개 통과, 새 재개 2개와 회차 이동 1개 실패했다. 위 앱 결함과 선택상자 locator를 수정했다.
- 전체 브라우저 검사 npm.cmd run test:e2e -- --workers=2: 36개 중 30개 통과·6개 실패(13.9분). 실패한 6개만 workers=1로 실행해 4개 통과(6.9분), 남은 회차·내보내기 2개를 다시 실행해 모두 통과(2.2분)했다. 한 번의 전체 36개 통과로 표기하지 않는다.
- 재개 테스트의 RSC 응답 종료 대기는 UI·재접속 검증으로 대신했고, 회차 테스트의 복수 alert 및 이미 열린 메모 영역을 다시 닫는 locator 오류를 수정했다. 긴 내보내기 시험은 다운로드·인쇄·권한 검증 후 정리 중 90초를 넘겨 해당 시나리오만 180초로 설정했다. 위험성평가 화면·뒤로가기 시험은 제한 변경 없이 재통과했다.
- 회차 브라우저 검증에서 이전 담당자·날짜·메모가 새 회차에 남지 않는지, 미저장 입력이 새 회차 시작을 막는지, 지난 문서 v1/v2·증빙·수행·미해결 위험과 새 회차 v1을 구분하는지 확인했다.
- 모바일 지도 최종 수정 후 npm.cmd run lint와 npm.cmd run build(TypeScript 포함) 재통과. npm.cmd run test:e2e -- tests/e2e/mobile-map.spec.ts tests/e2e/journey.spec.ts tests/e2e/task-cycles.spec.ts --workers=1 --output=test-results/mobile-final → 5개 통과(2.3분). 360px의 분야 제목·첫 행동 위치와 Tab 연결, 두 모션 설정, 1440px 기존 포커스·스크롤, 저장·재개·회차 흐름을 확인했다. 전체 36개와 추가 지도 2개를 분할 검증했으며 최종 수정의 관련 5개를 다시 통과했다.
- test-results/document-search-360.png, task-cycle-360.png, mobile-map-selection-360.png를 직접 열어 모바일 배치·정보 구분을 확인했다. 실제 휴대전화의 가상 키보드·저속망·화면낭독기와 실제 고객 참여 시험은 하지 않았다.
- 1차 빌드 후 전체 lint에서 파일 복구의 렌더 중 현재 시각 조회 1건을 발견했다. 서버 조회 시각을 반환하도록 수정한 뒤 전체 lint와 최종 프로덕션 빌드(TypeScript 포함)를 통과했다.
- npm.cmd run test:db -- --testTimeout=120000 → 7개 파일 73개 통과(132.52초). 병행 부하에서 한 차례 제한시간을 넘긴 백업·복원 시험도 단독 통합 실행에서 통과했다. 실제 Supabase·Auth·Storage·파일 원본 복구 결과는 아니다.

- 최종 git -c core.safecrlf=false diff --check 통과. 변경 문서 7개의 UTF-8·로컬 링크 19개 확인 통과.

### 실행 방법

- 최종 체험 미리보기: **http://127.0.0.1:3021/demo** → 새 체험 시작 → 사업장 질문 → 오늘의 한 가지 → 전체 지도 → 기록·자료·검토.
- APP_MODE=demo, AI_MODE=mock, OCR_MODE=mock, SAFETY_ENABLE_TEST_FIXTURES=0으로 npm.cmd run start -- --port 3021 실행. /demo HTTP 200, /api/testing/session POST 404 확인. 테스트 서버 3015는 종료하고 체험 서버 3021은 유지했다.
- 체험은 메모리 저장이라 서버 재시작 시 초기화될 수 있다. 가상 자료로만 확인한다. 실제 연결 설정과 0014까지 적용 순서는 [P2 설정](P2_SETUP.md)을 따른다.

### 실사용 차단 조건과 다음 작업

- 사용자도 Supabase 환경이 아직 없음을 확인했다. 실행 환경에 SUPABASE_URL/SUPABASE_PUBLISHABLE_KEY 설정과 .env.local이 없고 Docker 엔진도 실행되지 않았다. 실제 로그인/MFA·권한 철회·원본 파일 업로드/검사·복구는 미검증이다. 키는 채팅 대신 P2 설정 문서에 따라 로컬에 설정한다.
- 승인된 실제 법령 규칙 0개·세부 조문 미연결 73개의 기존 상태를 유지한다. 이번 회차에서 법령·교육시간·주기·기한을 새로 만들거나 승인하지 않았다.
- 실제 초보 고객의 과제 수행, 첫 고객 업종별 교육·선임·도급 등 전용 업무, 계정 복구·운영 배정·지원·보존 정책 실행은 남아 있다. 상품성·실사용 준비 완료로 보고하지 않는다.
- 다음 단계는 실제 시험 환경 연결과 파일 검사·복구 시험, 현장 고객의 반복 업무 검증 및 승인 범위에 맞춘 법령 규칙 확정이다.

## 2026-09-18 · 가독성·벤토 홈·설명형 작업 화면 구현

### 구현한 사용자 흐름

- 사용자 “계획대로 진행해봐” 승인에 따라 시각 재검토안을 구현했다. 기존 저장소·미커밋 변경·기록을 보존했으며 공개 배포·커밋·외부 발송은 하지 않았다.
- Pretendard v1.3.9 실제 글꼴과 OFL을 포함하고 명조/고딕 혼용을 없앴다. 제목·본문·입력·버튼·저장 상태의 크기를 역할별로 키우고 밝은 회백·흰 면·네이비 글자·파란 주요 행동으로 바꿨다.
- 홈은 다음 행동이 가장 큰 벤토 영역이며 분야 지도·실제 남은 확인을 보조로 표시한다. 실제 저장한 기록은 목록으로 연결한다. 예시나 임의의 완료 수치로 카드를 채우지 않는다.
- 위험성평가에 장소·작업자·기록의 설명용 SVG, 단계별 그림 펼침과 위험요인 기록의 수동 3컷 도움말을 추가했다. 설명 이동·닫기와 실제 입력·저장은 분리되고 모션 감소·키보드·포커스 복귀를 유지한다.
- 모바일은 단일 열로 읽고, 지도에서 미시작 업무는 펼쳐 본다. 저장 상태는 스크롤 중에도 확인할 수 있으며 입력/제목 이동 여백을 둔다. 기존 저장·실패·재개·버전·권한·법적 상태 계산은 유지한다.

### 변경 파일

- 글꼴·공통 디자인: `src/app/fonts/PretendardVariable.woff2`, `OFL.txt`, `README.md`, `src/app/layout.tsx`, `task-design.css`, `journey.css`, 신규 `readability.css`.
- 화면: `src/components/journey-home.tsx`, `journey-map.tsx`, `risk-assessment-form.tsx`, 신규 `work-scene.tsx`, `risk-story.tsx`.
- 검증·계획: 신규 `tests/e2e/readability.spec.ts`, `docs/plans/2026-09-18-visual-readability-implementation.md`, 시각 재검토안, `DESIGN.md`, `DECISIONS.md`, 이 진행 기록.

### 실제 실행·검증

- 공식 글꼴 다운로드는 처음 샌드박스 네트워크 제한으로 실패했고, 명시적 실행 권한을 받아 원본과 라이선스를 내려받았다. 약 2MB 원본 글꼴은 앱에서 자체 제공하며 외부 CDN 런타임 요청은 없다.
- `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run build` 통과. 최종 그림 전환·스크롤 여백 보강 후 빌드(TypeScript 포함)와 ESLint도 재통과했다.
- `npm.cmd test -- --maxWorkers=1`: 17개 파일 179개 통과. 서버/DB 변경은 없으며 DB 전용 검사는 이번 시각 변경에서 다시 실행하지 않았다.
- `npm.cmd run test:e2e -- tests/e2e/readability.spec.ts tests/e2e/journey.spec.ts --workers=1`: 4개 통과(1.2분). 실제 폰트 로딩·폰트 실패 대체, 홈 CSS 200% 확대, 360/1440px 넘침·axe, 수동 설명·키보드·모션 감소, 예시 미저장·입력 유지·저장 실패/재시도/재접속을 확인했다. 브라우저 메뉴를 통한 실제 기기 확대·화면낭독기 수동 시험은 별도로 하지 않았다.
- 최종 전체 `npm.cmd run test:e2e -- --workers=2`: **32개 모두 통과(9.1분)**. 사업장 입력 네 단계의 320/360/768/1440px·대비, 검색 복원, 자료 제출/보완/검토, 과거 버전·다운로드, 두 탭 수정 충돌, 다른 고객/역할 접근 차단과 신규 시각 설명 흐름까지 같은 최종 빌드에서 재검증했다. 테스트용 3015 서버는 실행기가 종료했다.
- `readability-home-360.png`, `readability-home-1440.png`, `readability-risk-360.png`, `readability-story-360.png`, `journey-home-1440.png`를 열어 글자·배치·그림을 확인했다. 변경 전 캡처는 `.cache/visual-before`에 보존했다. 동일한 위험성평가 v1 기록 준비 1/7 상태의 홈을 변경 전후로 비교했다. 실제 사용자 참여 비교 결과는 아니다.
- 최종 `git -c core.safecrlf=false diff --check` 통과. 변경 문서 6개의 UTF-8·로컬 링크 13개를 Node로 확인해 통과했으며 미리보기 `/demo` HTTP 200을 재확인했다.

### 실행 방법·미검증·다음 작업

- 새 미리보기: **http://127.0.0.1:3018/demo** → 새 체험 시작 → 사업장 정보 확인 → 홈 → 작업과 장소 정하기 → 위험성평가의 그림·예시 보기.
- `APP_MODE=demo`, `AI_MODE=mock`, `OCR_MODE=mock`, `SAFETY_ENABLE_TEST_FIXTURES=0`; `npm.cmd run start -- --port 3018` 실행. `/demo` HTTP 200, `/api/testing/session` POST 404 확인. 가상 체험 자료는 메모리 기반이다.
- 실제 초보자 5명 비교 시험·실기기/저속망 성능은 미실행이다. 다음은 대표 두 화면의 사용자 이해도 확인과 업무 회차 관리, 교육·선임 등 개별 설명 흐름 확장이다. 실제 Auth/Storage·파일 검사·복구, 승인 법령 규칙 0개·세부 조문 미연결 73개의 기존 미완료 범위는 유지한다.

## 2026-09-18 · 가독성·시각적 설명 방식 재검토

- 사용자 추가 요청: 구성·내용은 개선되었으나 폰트·글자 크기·디자인 가독성이 부족하므로 다른 디자인과 벤토 그리드·발표 자료 같은 설명 방식을 토론한다.
- 실제 CWD·Git 최상위를 확인하고 기존 코드·기록을 보존했다. 디자인·개발·운영 관점의 에이전트가 읽기 전용 검토·반론을 교환했다. 초보 고객 관점은 시나리오 검토이며 실제 고객 참여는 아니다.
- CSS·컴포넌트·1440px 홈과 360px 지도 캡처를 검토했다. 실제 폰트 공급 없이 글꼴 이름만 지정, 명조/고딕 제목 혼용, 10~14px 상태·설명, 긴 모바일 상세와 업무에 연결되지 않은 도식이 주요 개선 대상이다.
- 권고: 홈은 주 행동 하나를 크게 둔 벤토, 작업은 큰 질문·구체적인 그림·입력, 자료·이력은 목록. 한국어 산세리프 실제 공급과 역할별 글자 크기·행간을 우선 정하고 설명에 필요한 전환만 추가한다. 기존 크림·코랄·명조를 새 시안의 고정 제약으로 삼지 않는다.
- 변경 파일: `docs/plans/2026-09-18-visual-readability-review.md`, `docs/DESIGN.md`, `docs/PROGRESS.md`. 새 시각안은 제안이며 앱 코드는 변경하지 않았다. 이전 앱 테스트 결과를 새 디자인 검증으로 보고하지 않는다.
- 실제 검증: `git -c core.safecrlf=false diff --check` 통과. Node로 변경 문서 3개의 UTF-8와 로컬 링크 6개를 확인해 통과했다. 앱 테스트·새 시안 렌더링·고객 참여 시험은 이번 토론에서 실행하지 않았다.
- 다음 제안: 같은 데이터로 오늘 홈과 위험성평가 관찰·기록 장면을 비교한 뒤 적용 범위를 확장한다. 실제 초보자 이해도, 모바일·200% 확대·모션 감소, 저장/수행 구분을 검증한다. 실제 서비스 연결·법령 검증·회차 관리 등 기존 미완료 범위는 유지한다.

## 2026-09-18 · 안내 홈·전체 지도·입력·증빙 개편 구현

### 구현한 사용자 흐름

- 사용자의 “진행해봐” 승인으로 설계의 A~C를 구현했다. 현재 CWD·Git 최상위가 지정 저장소임을 확인했으며 기존 미커밋 변경·원본·이력을 보존했다. 커밋·공개 배포·외부 발송은 하지 않았다.
- 첫 이용 안내 → 사업장 질문 → 저장하고 다음 → 확인 → 오늘의 한 가지 업무 → 전체 지도 → 실제 입력·증빙·검토·보완을 연결했다. 미완성 프로필에서도 지도는 열리며 신규 업무 생성의 기존 서버 확인 기준은 유지한다.
- ‘오늘’은 등록 업무가 없는 고객에게도 첫 업무를 추천한다. 이후 저장된 개선·보완·준비 일정·초안에 따라 다음 행동을 제시한다. 전체 목록·검색은 별도 펼침으로 보존했다.
- ‘전체 지도’는 분야·업무·위험성평가 단계·저장본으로 연결되며 같은 내용을 목록으로도 볼 수 있다. 위험성평가 기록 준비, 미판단, 명시된 개선 필요와 검토 상태를 구분한다. 일반 업무의 실제 이행 완료율은 계산하지 않는다.
- 사업장 입력은 쉬운 질문과 보조 정보 펼침으로, 위험성평가는 7단계 안의 짧은 질문 묶음과 한 번에 한 위험요인으로 재구성했다. 단계 직접 이동과 전체 항목 직접 입력도 제공한다. 모름·빈칸·0·아직 수행하지 않은 사실을 구분한다.
- 안내 위치는 사용자·현장·업무별로 서버에 따로 저장한다. 저장하고 다음은 서버 저장 성공 후에만 이동한다. 저장 실패·미저장 이탈·다른 폼 제출·revision 충돌 방어를 유지했다. UI 위치 저장으로 업무 버전·수행·검토를 변경하지 않는다.
- 자료에는 목적·연결된 버전·증빙 예시·다음 행동을 표시하고 저장 내용→증빙→검토·보완→실제 수행 기록을 직접 연결했다. 예시를 실제 사실로 채우지 않고 파일 검사·증빙 내용·수행·접수를 각각 구분한다.
- 도움말 dialog의 Escape 닫기·포커스 복귀, 단계 전환과 모션 감소, 모바일 관계도·목록을 구현했다. 새로운 외부 라이브러리는 추가하지 않았다.

### 변경 파일

- 안내 정의·저장 기준: `src/domain/journey.ts`, `task-action-source.ts`, `src/server/demo-tasks.ts`, `journey-state.ts`, `journey-actions.ts`, `supabase/migrations/202609180009_guided_journey.sql`.
- 화면: `journey-home.tsx`, `journey-map.tsx`, `journey-help.tsx`, `journey-welcome.tsx`, `task-home.tsx`, `business-profile.tsx`, `risk-assessment-form.tsx`, `task-forms.tsx`, `task-detail.tsx`, `task-guidance.tsx`, `document-panel.tsx`, `workspace-form.tsx`, 회원 탐색·자료·검토 화면과 지도 경로, `src/app/journey.css`.
- 검증: `tests/unit/journey.test.ts`, 기존 요약 DB 테스트, `tests/e2e/journey.spec.ts`와 화면 변경에 따른 기존 회귀 동작. 구현 계획·제품·UX·디자인·개발 순서·결정·P2 설정·진행 기록.

### 실제 실행·검증

- TypeScript·ESLint·프로덕션 빌드 통과. 최종 요약 정의 버전 추가 시 숫자 리터럴 추론 오류로 빌드 1회 실패했고 반환 타입을 명시해 수정했다. 최종 앱 코드의 `npm.cmd run build`도 통과했다.
- 브라우저 검사 조정 후 `npm.cmd run lint`, `npm.cmd run typecheck`를 다시 실행했고 모두 통과했다.
- `npm.cmd test -- --maxWorkers=1`: 최종 요약 변경 후 17개 파일 179개 통과.
- `npm.cmd run test:db -- --testTimeout=120000`: 최종 3개 파일 52개 통과. 전체 7단계·보완 복귀, SQL/TS 공통 사례, 동일 시점 검토 상태, 안내 위치·민감자료·타 고객 차단과 원문 비노출을 확인했다.
- `npm.cmd run test:e2e -- tests/e2e/journey.spec.ts --workers=1`: 새 안내 흐름 2개 통과(40.1초). 첫 업무·저장/재개·실패 복구·예시 미입력·팝업 포커스·저장 상태 일치·360/1440px 및 axe 검사를 수행했다. 이후 첫 이용 안내와 요약 보강은 아래 전체 실행에서 다시 검증했다.
- 첫 전체 `npm.cmd run test:e2e -- --workers=2`: 30개 중 25개 통과(7.9분). 첫 안내 건너뛰기 글자 대비 문제 2건, 접힌 메모를 열지 않은 기존 테스트 2건, 검색 결과 복귀 시 목록 접힘 1건을 확인했다. 대비를 높이고 필터가 있는 주소에서 목록을 열도록 수정했으며 테스트는 실제 추가 메모 열기 동작으로 맞췄다.
- 수정 후 동일 명령의 전체 재실행: 29개 통과·1개 시간 초과(8.4분). 신규 안내 2개와 기존 저장·재열람·검색 복귀·과거 버전·다른 탭 충돌·권한·다운로드 검사는 통과했다. 남은 사업장 입력 검사는 4단계 × 4화면 폭의 axe 검사·캡처 도중 90초 제한에 걸려 이 테스트만 180초로 조정했다.
- 사업장 입력 검사 단독 재실행에서 진입 애니메이션 중간의 반투명 색상을 읽는 경합을 확인했다. 고정 대기나 대비 제외 없이 실제 유한 애니메이션 종료를 기다리도록 수정했다. `npm.cmd run test:e2e -- tests/e2e/business-profile.spec.ts -g '사업장 입력 네 단계' --workers=1 --output=test-results/profile-layout-final`: 1개 통과(16.1초). 320·360·768·1440px의 네 단계 모두 넘침·WCAG axe 검사를 통과했다. 최종 앱 빌드의 30개 시나리오를 전체 실행 29개 + 단독 재검사 1개로 검증했으며 한 번의 전체 30개 통과로 표기하지 않는다.
- `npm.cmd run catalog:check`: 후보 78개·업종 18개·분류 8개와 문서/CSV 일치. 최종 문서 9개의 UTF-8·로컬 링크 20개 확인, `git -c core.safecrlf=false diff --check` 통과.
- `test-results/journey-home-1440.png`, `journey-map-360.png`, `profile-step-1-320.png`를 직접 열어 시각 확인했다. 실제 초보 고객 참여 시험은 하지 않았다.

### 실행 방법

- 최종 빌드 미리보기: **http://127.0.0.1:3017/demo** → 새 체험 시작 → 사업장 질문과 저장하고 다음 → 오늘 → 전체 지도 → 기록·자료·검토. 테스트 서버 3015는 실행기가 종료하고 체험 서버 3017은 유지한다.
- 실행 설정: `APP_MODE=demo`, `AI_MODE=mock`, `OCR_MODE=mock`, `SAFETY_ENABLE_TEST_FIXTURES=0`; 명령 `npm.cmd run start -- --port 3017`. `/demo` HTTP 200, 테스트 역할 전환 `/api/testing/session` POST 404를 확인했다.

### 미연동·미검증·다음 작업

- 가상 체험은 메모리 저장이므로 서버 재시작 시 초기화될 수 있다. 실제 서비스에는 0009까지 별도 시험 환경에 적용하고 Auth/Storage/파일 검사·복구를 연결 검증해야 한다.
- 실제 승인 규칙 0개·세부 조문 미연결 73개와 실제 초보자 5명 사용성 시험 대기는 유지한다. 법령·법정 일정·법적 판정 규칙을 이번에 변경하거나 승인하지 않았다.
- 다음은 위험성평가 회차와 문서 버전을 분리하고 이전 미해결 위험을 연결하는 작업이다. 교육·선임·도급·점검/건강의 개별 구조화 안내는 후속 범위로 보존한다.

## 2026-09-18 · 능동 안내·전체 지도 개편 설계

### 정리한 사용자 흐름과 구현 상태

- 사용자는 현 UI가 초보자에게 첫 행동과 전체 흐름을 알려주지 못한다고 지적했다. 최신 요구를 반영해 오늘의 한 가지 행동 → 짧은 질문·실행 안내 → 저장·증빙 → 전체 지도에서 위치 확인 → 보완의 연결 흐름을 설계했다. 이번 단계는 문서 변경이며 앱 코드 변경은 없다.
- 디자인·개발·운영 관점의 기존 작업자가 읽기 전용으로 검토했다. 코드 대조 결과 등록 업무가 없는 고객의 첫 업무 추천 부재, 긴 입력, 접힌 수행 안내, 전체 위치·재개 연결 부족을 확인했다. 실제 부서·고객 참여 검증은 아니다.
- 전체 지도는 분야·업무·단계·자료 관계를 보여준다. 기록 준비·미판단·명시된 개선 필요·실제 수행·검토·접수를 별도로 표시한다. 현재 구조화되지 않은 업무에는 근거 없는 완료율을 만들지 않는다.
- 단계 안내와 전체 지도를 기본 추천안으로 삼고, 대화 중심·자유 탐색 중심 안의 장단점을 비교했다. 첫 안내·도움말 패널·저장 결과 전환과 모바일·키보드·모션 감소를 함께 정리했다.
- 홈·지도·사업장 입력·위험성평가·자료 연결을 함께 개편하는 A~C 묶음을 제안했다. 기존 다음 순서였던 회차 관리보다 이번 UX 개편을 먼저 두고, 후속 업무와 실제 연결·법령 검증의 미완료 항목은 보존했다.

### 변경 파일·실제 확인

- 신규 설계: `docs/plans/2026-09-18-guided-safety-journey-design.md`.
- 요구·화면·우선순위 반영: `docs/01_PRODUCT.md`, `03_UX.md`, `DESIGN.md`, `05_PHASES_TESTS.md`, `DECISIONS.md`, `PROGRESS.md`.
- 실제 CWD와 `git rev-parse --show-toplevel`이 지정 저장소와 일치함을 확인했다. 기존 미커밋 변경을 보존했다.
- 기존 컴포넌트·도메인·요약 조회 계약과 진행 기록을 읽고, 토스 공식 라이팅 원칙과 W3C 팝업·모션 접근성 문서를 참고했다. 참고 링크는 설계안에 기록했다.
- `git -c core.safecrlf=false diff --check` 통과. Node로 변경한 문서 7개의 UTF-8 읽기·깨진 문자와 로컬 링크 16개, 신규 설계안의 줄 끝 공백을 확인했고 모두 통과했다.
- 이번에는 앱 테스트·빌드·브라우저 사용성 검증을 재실행하지 않았다. 아래 176개 단위·50개 DB·28개 E2E 결과는 2026-09-17 구현의 검증이다.

### 실행 방법·미검증·다음 작업

- 설계 검토는 위 Markdown 문서로 가능하다. 새 화면은 아직 로컬 앱에 반영되지 않았다. 기존 앱은 `npm.cmd run dev`로 실행할 수 있으며 2026-09-17의 미리보기 주소는 `http://127.0.0.1:3016`이다. 해당 서버 생존 여부는 이번 단계에서 재확인하지 않았다.
- 상세 설계 확인 후 A. 공통 상태·오늘·지도, B. 사업장 입력·위험성평가, C. 자료·증빙·보완 연결 순서로 구현·검증한다. 같은 저장본의 상태 일치, 신규 고객의 첫 업무, 저장 실패·재개·권한·버전 보존을 인수 기준으로 삼는다.
- 실제 초보자 5명 사용성 검증과 실제 법령 승인 규칙 0개·세부 조문 미연결 73개 상태를 유지한다. 실제 Auth/Storage·파일 검사·복구 검증은 여전히 미완료이며 UX 개편으로 완료 처리하지 않는다.

## 2026-09-17 · 다음 행동·미확인 정보·저장 재열람

### 구현한 사용자 흐름

- 사용자 승인 계획의 첫 묶음을 구현했다. 기존 미커밋 변경과 이전 기록을 보존했고 커밋·공개 배포·외부 발송은 하지 않았다. 디자인·개발·운영 관점의 기존 작업자에게 독립 범위를 나누고 통합·교차 검토했다. 실제 고객 사용성 검증은 아니다.
- 홈의 ‘다음으로 할 일’은 최대 3행과 총 남은 수·나머지 펼침을 제공한다. 미해결 개선조치 → 보완 의견 → 경과/임박한 준비 목표 → 미확인 정보 → 초안 순서로 구체적 동작과 이유를 표시한다. 경영책임자·관리체계 진입도 초기 목록에 노출한다.
- 최신 저장 버전·작성자 확인 상태를 출처로 표시한다. 자료 검토 중·완료여도 남은 개선·재확인을 숨기지 않으며 높은 위험도만으로 긴급 상태를 만들지 않는다. 준비 목표일은 법정 기한과 구분한다. 서로 다른 요청에서 읽힌 검토 상태와 미확인 최신본이 어긋나면 후속 확인으로 남긴다.
- 사업장 추가 질문마다 안정 ID·이유·해당 단계·첫 미확인 필드를 연결한다. 0과 미응답을 유지한다. 잘못된·이미 해소된 질문 주소는 첫 단계로 돌아간다.
- 입력 변경·저장 중·저장 완료·실패를 표시하고 링크·새로고침/닫기·지원 브라우저 뒤로가기 이탈을 확인한다. 미저장·저장 중 다른 폼의 검토 요청 등은 먼저 임시 저장하도록 안내한다. 긴급 안내는 새 창으로 즉시 열며 입력 원문은 브라우저 영구 캐시에 보관하지 않는다.
- 실제 입력 변경이 시작된 시점의 revision을 고정한다. 미저장·저장 중 다른 갱신이 와도 저장 기준을 바꾸지 않고, 자체 저장 성공 또는 원래 값 복원 후 다음 편집에서 새 기준을 사용한다. 정상 내용 확인→보완 흐름을 유지한다.
- 입력 영역과 저장본을 구분하고 저장 내용·버전 이력을 펼쳐 볼 수 있게 했다. 새 답변에는 당시 질문 ID·원문·정의 버전을 저장한다. 원문 없는 기존 답변은 현재 질문을 추정하지 않는다. 화면을 연 뒤 질문이 바뀌었으면 서버가 입력을 보존한 채 저장을 거부한다.
- 실제 모드의 일괄 요약 RPC는 현장·민감자료·배정/MFA 권한을 적용하고 원문·검토 의견 없이 최소 집계만 반환한다. 조회마다 요약 감사 1건을 남긴다. 신규 0008은 함수만 추가하며 기존 데이터를 변경하지 않는다.

### 실제 실행·테스트 결과

| 명령·검증 | 결과 |
|---|---|
| `npm.cmd test -- --maxWorkers=1` | 최종 도메인 변경 후 16개 파일 176개 통과. 질문 원문/버전·배포 중 질문 변경·홈 순서/검토와 조치 분리·불일치 상태 보수적 안내 포함 |
| `npm.cmd run test:db -- --testTimeout=120000` | PGlite 3개 파일 50개 통과. SQL/TS 공통 사례, 최신본, 다른 현장·읽기전용·민감자료·검토자 배정/MFA, 비공개 helper 실행 차단, 요청당 감사 1건 |
| 코드 검사 | 저장 보호·질문 식별값·revision 보강 후 최종 전체 ESLint·TypeScript·프로덕션 빌드 통과 |
| 첫 `npm.cmd run test:e2e` | 26개 중 25개 통과(8.3분). 새 개선조치 시나리오 1개는 도움말이 포함된 입력 레이블을 exact로 찾는 선택자 실패. 화면의 입력 존재 확인 후 선택자 수정 |
| 최종 `npm.cmd run test:e2e -- --workers=2` | 최종 빌드에서 전체 28개 통과(6.1분). 직접 입력칸 포커스·0 보존, 기본3행/전체펼침, 검토 대기/완료 후 개선 표시, 저장 실패·복구·긴급 새창·링크/뒤로가기 보호, 미저장/저장 중 검토요청 차단, 두 탭 충돌·입력/상대 저장본 보존 및 기존 회귀 검증 |
| 화면 확인 | 360px·1440px 홈 캡처 육안 확인. 첫 브라우저 실행의 기존 화면/접근성·저장/과거 버전/내보내기·인증 경계 회귀는 통과 |
| `npm.cmd run catalog:check`, `git -c core.safecrlf=false diff --check` | 후보 78개·문서·CSV 일치, 공백 오류 없음 |
| 로컬 미리보기 | 최종 빌드를 `npm.cmd run start -- --port 3016`으로 실행. `http://127.0.0.1:3016` HTTP 200, 테스트용 역할 세션 API POST 404 확인. APP_MODE=demo, AI/OCR=mock, 테스트 fixture 비활성 |

최종 28개는 같은 최종 빌드에서 전체 재실행한 결과다. 첫 선택자 실패도 위에 보존했다. 테스트 실행기가 3015 서버를 종료했으며 가상 체험 미리보기 3016은 유지한다. 실제 기기·고객 참여 검증을 자동 브라우저 결과로 대체하지 않는다.

### 변경 파일·실행 방법

- 홈·입력: `task-actions.ts`, `task-action-source.ts`, `workplace-profile.ts`, `task-home.tsx`, `task-board.tsx`, `business-profile.tsx`, 회원 홈 경로.
- 저장·재열람: `workspace-form.tsx`, `task-forms.tsx`, `task-detail.tsx`, `document-panel.tsx`, `risk-assessment-form.tsx`, `tasks.ts`, `workspace.ts`, `task-actions.ts` 서버, `risk-export.ts`, `shell.tsx`, `task-design.css`.
- 권한·요약: `adapters/workspace.ts`, `server/demo-tasks.ts`, `supabase/migrations/202609170008_task_action_sources.sql`.
- 검증: 신규 task-actions/task-action-source 단위·DB/공통사례, workplace-profile/tasks/task-repository 보강, next-actions/save-state E2E와 기존 버전 이력·목록 테스트 보강.
- 계획: `docs/plans/2026-09-17-next-actions-design.md`, 제품·UX·디자인·개발순서·결정·P2설정·진행 기록.
- 실행: `npm.cmd run dev` → `/demo`에서 새 체험 → 사업장 입력 확인 → 홈의 다음 행동/미확인 정보 → 업무 기록·임시 저장 → 재열람. 실제 모드는 최신 앱보다 먼저 0008까지 별도 시험 환경에 적용·연결 검증해야 한다.

### 미연동·미검증 범위와 다음 작업

- 가상 체험은 메모리 저장이다. 실제 Supabase·파일 검사·운영 백업/복구·장기 보존은 계속 미연동/미검증이며 PGlite와 HTTP 대역 검증을 실서비스 연결로 취급하지 않는다.
- 실제 법령 승인 규칙 0개, 세부 조문 미연결 73개, 법률 전문가·현장 고객 검토 대기를 유지한다. 반복 회차·교육·선임·도급 전용 흐름은 이번 첫 묶음의 완료 범위가 아니다.
- SPA 뒤로가기 이탈 보호는 Navigation API 지원 브라우저에서 제공한다. 구형 브라우저·실제 iOS/Android·보조기기 검증은 남는다. 실제 초보 담당자 5명 참여 사용성 시험도 대기다.
- 다음 작업은 위험성평가 회차와 문서 수정 버전 분리·이전 미해결 위험 연결이다. 법령 원장/승인 규칙과 실제 인증·파일·복구 검증을 병행하고 이후 교육 → 선임·역할 → 도급 → 점검·건강으로 확장한다. 과거 수행·참석·완료 사실은 자동 복사하지 않는다.

## 2026-09-17 · 로컬 미리보기 시작

- 사용자 접속 요청으로 최신 빌드를 `npm.cmd run start -- --port 3016`으로 실행했다. 접속 주소는 `http://127.0.0.1:3016`이며 HTTP 200 응답을 확인했다.
- APP_MODE=demo, AI_MODE=mock, OCR_MODE=mock, SAFETY_ENABLE_TEST_FIXTURES=0으로 실행했다. 가상 자료를 사용하는 로컬 체험이며 공개 배포가 아니다. 앱 코드 변경과 기능 테스트 재실행은 없고 이 기록만 갱신했다.

## 2026-09-17 · 위험성평가 버전별 내보내기

### 구현한 사용자 흐름

- 지정 작업 폴더의 실제 CWD·Git 최상위 경로와 기존 미커밋 변경을 확인했다. 남은 기능 중 내보내기를 먼저 구현했으며 기존 파일을 초기화·이동·커밋하지 않았다. 작업 중 추가된 아래 TEST 조사 기록도 보존했다.
- 위험성평가를 저장한 뒤 ‘이 버전 내려받아 보관하기’에서 인쇄용 HTML과 원본 기록 JSON을 내려받는다. 과거 버전을 선택하면 그 버전의 사본을 받으며, 작성 중인 미저장 내용은 섞이지 않는다.
- HTML은 모든 평가 항목·위험요인·미입력/미확인 값·기존 답변·메모를 펼쳐 제공한다. 브라우저 인쇄 메뉴로 출력하거나 PDF로 저장할 수 있다. JSON에는 저장 원문, 구조화 기록과 원문 대조용 SHA-256이 포함된다.
- 선택 버전의 현재 작성자 확인·공개 검토 의견·증빙 목록을 표시한다. 수행 기록은 내보낸 시점의 문서 전체 이력임을 명시하고 과거 버전 작성 당시 상태나 특정 평가 회차의 수행으로 추정하지 않는다.
- 파일 열기·내용 확인 → 증빙 원본과 함께 보관 → 실제 보관 위치 기록을 안내한다. 다운로드는 수행·승인·기관 제출·접수·장기 보존 완료로 저장하지 않는다. 첨부파일 원본은 별도 다운로드 대상이다.
- 실제 모드는 기존 인증·문서/민감자료 권한·소속 버전 검사·감사 조회 RPC를 재사용한다. 새 주소를 세션 갱신 경로에 포함했다. 응답은 private/no-store 첨부파일이며 운영자 내부 메모·비공개 파일 경로를 제외하고 HTML 입력을 이스케이프한다.

### 실제 실행·테스트 결과

| 명령·검증 | 결과 |
|---|---|
| `npm.cmd test -- --maxWorkers=1` | 14개 파일, 137개 통과. 원문·빈 값·과거 버전 보존, 비공개 정보 제외, HTML 입력 이스케이프, 인증·읽기 권한·소속 버전 경계 포함 |
| `npm.cmd run test:db -- --testTimeout=120000` | PGlite DB 2개 파일, 24개 통과(15.91초). 기존 권한·감사·불변 버전·복구 검사 재실행. 새 마이그레이션 없음 |
| `npm.cmd run lint` | 전체 ESLint 통과. 이후 수정한 proxy·요약 컴포넌트·새 E2E의 집중 ESLint도 최종 통과 |
| `npm.cmd run typecheck`, `npm.cmd run build` | TypeScript 검사 통과. 세션 갱신 경로 보강 후 최종 프로덕션 빌드 재실행 통과. 동적 다운로드 경로 생성 확인 |
| `npm.cmd run test:e2e` | 전체 20개 중 기존 19개 통과, 새 1개는 저장 후 textarea를 찾는 exact label 선택자에서 시간 초과(6.4분). 접근성 스냅샷에서 입력칸이 정상임을 확인하고 textbox 역할+이름 선택자로 수정 |
| `npm.cmd run test:e2e -- tests/e2e/risk-export.spec.ts tests/e2e/p2-boundaries.spec.ts` | 최종 빌드에서 수정한 내보내기·인증 경계 2개 통과(30.5초). 실제 HTML/JSON 다운로드, v1 원문 유지·v2 보완/검토 대기, 미저장 내용 제외, 타 고객·미배정·비로그인 404와 배정 검토자 200, 수행·접수 상태 불변 확인 |
| 화면·인쇄 검증 | 보고서 360/768/1440px 가로 넘침 없음. 보고서와 모바일 다운로드 영역 axe WCAG 2 A/AA·2.1 AA·2.2 AA 통과. 인쇄 매체에서도 위험요인 표시 확인 |
| PDF 렌더링 | Edge 인쇄 PDF 6쪽 생성 후 pypdfium2로 모든 쪽을 PNG 렌더링하여 육안 확인. 한글·줄바꿈·페이지 분할·내용 표시 확인. pypdf의 쪽별 텍스트 추출도 확인 |
| `npm.cmd run catalog:check`, `git -c core.safecrlf=false diff --check` | 기존 78개 후보·문서·CSV 일치, 공백 오류 없음 |

브라우저 성공 결과는 전체 실행의 기존 19개와 최종 재실행의 신규 내보내기 1개를 합친 범위다. 최종 20개 전체를 한 번에 다시 실행한 결과로 표시하지 않는다. 생성 사본·인쇄 PDF·쪽별 PNG는 Git에서 제외되는 `test-results/risk-export-*`에 있다. 테스트 서버는 실행기가 종료했으며 상시 미리보기 서버는 이번에 시작하지 않았다.

### 변경 파일·실행 방법

- 생성·표시: `src/domain/risk-export.ts`, `risk-record-fields.ts`, `src/components/risk-assessment-summary.tsx`, `document-panel.tsx`.
- 다운로드·인증: `src/app/api/documents/[id]/export/route.ts`, `src/proxy.ts`.
- 검증: `tests/unit/risk-export.test.ts`, `risk-export-route.test.ts`, `tests/e2e/risk-export.spec.ts`.
- 기록: `docs/plans/2026-09-17-risk-export-design.md`, `docs/DECISIONS.md`, 이 진행 기록.
- 실행: `npm.cmd run dev` → 가상 체험 → 사업장 정보 확인 → 위험성평가 관리 시작 → 가상 시험 내용 임시 저장 → 저장 자료의 ‘인쇄용 파일’ 또는 ‘원본 기록(JSON)’. 과거 버전 링크에서도 같은 동작을 확인한다.

### 미연동·미검증 범위와 다음 작업

- 데모 자료는 가상 입력·메모리 저장이며 재시작 시 사라질 수 있다. 다운로드 사본은 선택한 자료이며 서비스 전체 복원 파일이 아니다. 서버 직접 PDF 생성·첨부 원본 묶음 다운로드·전자서명·다운로드 수신 확인은 구현하지 않았다.
- 실제 Supabase Auth/Storage·파일 검사·다중 연결·장기 보존·운영 복구는 계속 미연동/미검증이다. 이번 실제 모드 검사는 서버 경계 시험 대역과 PGlite이며 실서비스 연결 결과가 아니다.
- 법령·보존기간·기산 자동 계산을 변경하지 않았다. 실제 법령 적용 승인 규칙 0개, 세부 조문 미연결 73개, 현장 사용자 시험·법률 전문가 검토의 미완료 상태를 유지한다.
- 다음 우선순위: 위험성평가 반복 회차 원장과 이전 미해결 위험의 연결(수행일·참여자·이행 사실 자동 복사 금지), 업종별 위험 발굴 안내·고시 정합성 검토. 이후 위험 작업·긴급 사고 흐름 점검과 교육 전용 흐름을 진행한다.
- 실제 외부 연결·보존/복구 검증과 기존 대화 탭의 작업 공간 소속 정리는 별도 미완료 항목으로 유지한다. 이번 작업은 지정 폴더에서 단독 수행했고 공개 배포·외부 발송은 하지 않았다.

## 2026-09-17 · TEST 잔여 파일·용량 확인

- 사용자 요청으로 `C:\Users\EKR\Documents\TEST`의 숨김·Git 제외 파일까지 읽기 전용으로 조사했다. 총 4,215개 파일, 4,397,620,323바이트(약 4.40 GB / 4.10 GiB)를 집계했으며 접근 오류와 제외된 연결 경로는 없었다.
- 안전관리 웹 프로젝트의 고유 파일명·텍스트 식별자와 `node_modules`, `.next`, Supabase·브라우저 테스트 산출물 디렉터리를 확인했으나 이 프로젝트 전용 잔여 파일은 발견되지 않았다. ZIP 6개도 추출 없이 내부 경로를 확인했으며 관련 프로젝트 파일명이 없었다.
- 주요 용량은 재해예방기술지도 프로그램의 `reports` 약 1,572.55 MiB, `downloads` 약 1,094.99 MiB, Git 이력 약 499.63 MiB, `samples` 약 391.12 MiB, `backups` 약 314.67 MiB, `output` 약 185.22 MiB였다. 프로그램 소스·DB·보고서·백업·설정·이력·캐시 모두 보존했다.
- 삭제 대상은 확인되지 않아 TEST에서 수정·이동·삭제한 파일은 0개다. 조사 전후 Git 상태는 모두 깨끗했다. 대화 탭의 TEST 소속은 안전관리 프로젝트 파일의 중복 저장을 의미하지 않는다.
- 변경 파일은 이 진행 기록뿐이다. 파일 변경이 없는 읽기 전용 조사이므로 프로그램 실행·기능 테스트는 하지 않았다. 기존 개발 후속 작업과 외부 연결 대기는 유지한다.

## 2026-09-17 · 중단 지점 확인·작업 폴더 연결 복구

- 직전 중단 지점은 Orca의 프로젝트/작업 공간 연결 확인이었다. 9월 16일 기존 폴더 등록은 성공했지만 작업 공간 인식과 왼쪽 표시를 검증하기 전에 중단됐다.
- 실제 작업 경로는 `C:\Users\EKR\Downloads\orca-safety-app-kit-v1\orca-safety-app-kit`이다. 위험성평가 코드·조사 문서·테스트와 기존 미커밋 변경을 확인했다. `C:\Users\EKR\Documents\TEST`는 별도 프로젝트이며 Git 변경 사항이 없다. 파일 이동은 필요하지 않았다.
- 현재 Windows 사용자와 저장소 소유자의 차이로 Git의 `dubious ownership` 오류가 발생했다. 사용자 전역 Git 설정의 `safe.directory`에 위 프로젝트의 정확한 경로만 추가했다. 이후 일반 `git rev-parse --show-toplevel`과 Orca 작업 공간 조회가 정상 동작했다.
- Orca 작업 공간 이름을 `orca-safety-app-kit`으로 지정하고 해당 공간에서 이 문서를 열었다. `orca worktree current --json`의 실제 경로와 데스크톱 화면의 왼쪽 선택 표시를 확인했다.
- 기존 실행 중 대화 터미널은 여전히 `TEST` 작업 공간 소속이다. 현재 CLI의 터미널 명령과 탭 메뉴에는 다른 작업 공간으로 이동하는 항목이 확인되지 않았다. 기존 대화 탭의 소속 변경은 미완료이며, 이름 변경만으로 이동했다고 취급하지 않는다. 실제 파일 작업은 계속 위 지정 경로에서 수행한다.
- 3016 포트의 미리보기 서버는 현재 실행 중이 아니다. 9월 15일의 테스트 결과는 아래 기록으로 보존하며 이번에는 앱 코드 변경·기능 테스트 재실행·서버 재시작을 하지 않았다.
- 이번 변경 파일: `AGENTS.md`의 작업 경로 지침과 이 진행 기록. 별도 환경 변경: 위 경로의 Git 신뢰 설정, Orca 작업 공간 표시 이름·문서 열기.
- 검증: 실제 CWD/Git 최상위 경로, 위험성평가 파일 존재, 두 저장소의 Git 상태, Orca 프로젝트 설정·작업 공간·터미널 연결, 데스크톱 왼쪽 표시, 미리보기 포트 상태 확인. 문서 변경 후 `git diff --check` 실행.
- 다음 작업: 기존 대화의 작업 공간 소속 정리, 위험성평가 내보내기·보존·반복 회차와 업종별 안내, 교육 전용 흐름, 미연결 73개 후보의 세부 조문 검토. 실제 인증·파일 저장·검사 및 운영 복구의 미완료 상태를 유지한다.

## 2026-09-15 · 초보자 안내와 위험성평가 실행 기록

### 구현한 사용자 흐름

- 업무 상세의 실제 입력·증빙·수행 기록을 법령 근거보다 위에 배치했다. 법적 근거는 확인 상태를 표시하고 기본 접힘으로 제공한다. 신규/기존 업무, 과거 자료 보기, 권한과 검토 중 편집 제한을 유지한다.
- 기존 78개 검토 후보와 자체 서식 3개 모두에 개별 목적·첫 행동·진행 순서·실제 증빙·마무리 확인·모를 때의 도움 안내를 작성했다. 001/002와 등록 서식은 등록 준비로 구분하며, 신규 업무 목록에는 기존대로 포함하지 않는다.
- REVIEW-003에 준비·방법/교육 → 일정 알림·근로자/대표 참여 → 여러 위험요인 찾기 → 기준에 따른 위험 판단 → 개선 계획과 실제 조치 → 조치 후 잔여 위험 확인 → 실제 실시 기간·결과 공유·보관·다음 확인을 연결하는 7단계 입력을 구현했다.
- 모르는 사실은 미확인으로 남긴다. 설명용 예시·참석자·측정값·날짜를 자동 생성하지 않는다. 사전 교육 상태와 실제 교육, 대표의 요청과 실제 참여, 조치 계획과 이행·효과 확인을 분리한다. 미래 수행일·중복 위험요인·역전된 조치 확인일을 검증한다.
- 위험요인별 담당·목표일·실제 조치·확인자·잔여 위험을 보존한다. 처음 허용 가능으로 판단했어도 새 잔여 위험이나 추가 조치가 있으면 미해결로 남긴다. ‘기록 갖춤’은 법적 완료·현장 안전·사람 검토·기관 제출로 바뀌지 않는다.
- 기존 자료 버전에 구조화된 평가 기록을 함께 저장한다. 과거 버전을 유지하고 타 업체 접근을 차단하며, 자료 저장만으로 별도 수행 기록이나 제출·접수 사실을 생성하지 않는다. 과거 공통 메모·답변도 보존한다.
- 준비 단계에 공식 소규모 안내서와 2026 개정 안내를 연결한다. 법 제36조, 시행규칙 제37조~제37조의4 등 8개 위험성평가 근거 묶음을 반영했다. 개정 시행일(6월)과 확인한 현행 전체 시행본(8월)을 구분한다.
- 초보 사업주도 이해하고 실행할 수 있어야 한다는 원칙을 AGENTS·제품·UX·디자인 문서에 적용했다. 모든 기존 항목의 편집 검토 대장과 전용 기능 후속 순위를 남겼다.

### 실제 실행·테스트 결과

| 명령·검증 | 결과 |
|---|---|
| `npm.cmd test -- --maxWorkers=1` | 12개 파일, 123개 단위 테스트 통과. 이후 교육·대표·실시 기간 보강 및 완전 기록 사례 추가 후 아래 위험성평가 집중 검사 수행 |
| `npm.cmd test -- --maxWorkers=1 tests/unit/risk-assessment.test.ts` | 최종 위험성평가 8개 통과. 전체 단위 사례 수는 124개이며 최종 124개 전체를 다시 실행하지는 않음 |
| `npm.cmd run test:db -- tests/integration/tasks.test.ts --testTimeout=120000` | DB 14개 통과(8.67초). 구조화 기록 왕복·과거 버전·조직 권한·상태 분리 확인. 기존 permissions 파일은 이번에 재실행하지 않음 |
| `npm.cmd run lint` | 전체 ESLint 통과. 이후 테스트 선택자 수정은 동작 검증과 최종 집중 ESLint로 확인 |
| `npm.cmd run build` | 최종 기능 코드의 프로덕션 빌드·TypeScript 검사 통과 |
| `npm.cmd run test:e2e` | 19개 중 15개 통과, 4개 테스트 선택자/기존 기대값 실패(6.7분). 구 조문 URL·기본 펼침 기대 2곳, 선택 상자 exact label 1곳, Next 경로 알림과 오류 알림 중복 1곳 |
| `npm.cmd run test:e2e -- tests/e2e/business-profile.spec.ts:60 tests/e2e/catalog.spec.ts:13 tests/e2e/risk-assessment.spec.ts` | 수정 후 나머지 4개 모두 통과(1.1분). 제품 코드는 변경하지 않았으며 19개 시나리오 전체의 성공 결과 확보 |
| 위험성평가 화면 | 7단계 × 360/768/1440px 가로 넘침 검사, 360px 각 단계 WCAG 2 A/AA·2.1 AA·2.2 AA axe 검사 통과 |
| 기존 법령 상세·사업장 입력 | 320/360/768/1440px 표시, 키보드 펼침, 입력·증빙이 근거 앞에 있는 DOM 순서 검사 통과 |
| `node test-results/inspect-layout.mjs` | 일반 미리보기 3016에서 등록→업무→위험성평가 입력, PC/모바일 화면 캡처·직접 시각 확인, 넘침 없음, 테스트 계정 API 404 확인 |
| `npm.cmd run catalog:check`, `git diff --check` | 기존 78개 원본 후보·CSV 일치, 공백 오류 없음 |

### 변경 파일

- 원칙·조사: `AGENTS.md`, `docs/01_PRODUCT.md`, `03_UX.md`, `DESIGN.md`, `DECISIONS.md`, `LEGAL_PREPARATION.md`, `BEGINNER_SERVICE_PRINCIPLES.md`, `BEGINNER_GUIDANCE_REVIEW.md`, `RISK_ASSESSMENT_RESEARCH.md`, 이 진행 기록.
- 안내·근거: `src/domain/task-guidance.ts`, `legal-basis.ts`, `src/components/task-guidance.tsx`, `legal-basis.tsx`, `task-detail.tsx`.
- 위험성평가: `src/domain/risk-assessment.ts`, `tasks.ts`, `src/components/risk-assessment-form.tsx`, `risk-assessment-summary.tsx`, `task-forms.tsx`, `src/app/task-design.css`. 기존 저장 RPC·DB 권한을 재사용하며 새 마이그레이션은 없음.
- 검증: `tests/unit/risk-assessment.test.ts`, `task-guidance.test.ts`, `legal-basis.test.ts`, `tests/integration/tasks.test.ts`, `tests/e2e/risk-assessment.spec.ts`, `legal-basis.spec.ts`, `business-profile.spec.ts`, `catalog.spec.ts`.

### 병렬 작업 결과

- Orca Run `run_6e74f0ca3254`: 실제 GPT-6 Astra / medium 3개 작업자. 공식 조사 `task_a602cca193e9`, 전체 안내 `task_2ce29d70a696`, 화면 우선순위·원칙 `task_4cdfe1cd0a33` 모두 성공 보고 후 release. `worker-list --terminal-state reclaimable` 결과 0개, released 3개.
- Orca의 현재 UI 작업 폴더와 대상 저장소가 달라 모든 지시에 대상 절대 경로와 다른 폴더 수정 금지를 명시했다. 입력창에 남은 기존 지시를 확인 후 제출했으며 중복 작업자를 만들지 않았다. 조정자가 위험성평가 입력·저장 계약과 통합·브라우저 검증을 맡았다.

### 실행 방법·미연동·미검증 범위

- 현재 일반 미리보기: `http://127.0.0.1:3016`. 체험 시작 → 사업장 정보 확인 → 위험성평가와 개선 추적 → 관리 시작. 다시 실행하려면 `npm.cmd run build` 후 demo/mock 설정으로 `npm.cmd run start -- --port 3016`.
- 체험은 가상 입력과 메모리 저장이다. 실제 파일은 받지 않고 가상 증빙만 연결한다. 실제 Supabase/Auth/Storage·파일 검사, 장기 보존·운영 복구, 기관 제출은 미연동이다.
- 첫 전용 도구는 3단계 위험 판단 기록이며 모든 평가방법·모든 산업의 작업별 안전조치를 구현한 것은 아니다. 한 자료의 기존 20,000자 제한과 최대 30개 위험요인 범위를 유지한다. 반복 회차 전용 원장·대량 내보내기·법정 일정 자동 알림·전자 참여 확인은 후속 작업이다.
- 기존 전체 항목 안내는 편집 검토다. 73개 후보는 세부 조문 연결이 검토 중이며 승인된 법적 적용 실행 규칙은 0개다. 위험성평가도 현행 규칙과 구 고시의 시기·상시 갈음·보존 기산 등 추가 검토 쟁점이 남았다.
- 실제 초보 사업주와의 사용성 시험, 현장 확인, 법률 전문가 승인·전 산업 의무 전수 검증을 이번 로컬 검사로 대신하지 않는다. 앞 단계의 미완료 외부 연결·보존·법령 검토 항목을 유지한다.

### 다음 작업

1. 위험성평가 법정 기록 항목의 내보내기·보존·반복 회차와 업종별 위험 발굴 안내를 보강하고, 고시 정합성 쟁점을 검토한다.
2. 위험 작업·긴급 사고 흐름을 우선 점검한 뒤 교육 대상을 고르는 단계부터 실제 교육·참석·미참석 후속까지 전용 모듈을 확장한다.
3. 남은 조문을 항목별 적용 사실·예외·시행일·증빙·보존과 연결하고 승인된 규칙만 실제 의무 판단에 사용한다. 실사용 연결과 초보 사용자 검증을 함께 진행한다.

## 2026-09-15 · 사업장 정보 선입력

- 등록정보·업종·실제 작업·인원·고용관계를 4단계로 입력하고 확인한 뒤 관련 업무를 표시한다. 초기 데이터나 기존 탐색 조건은 자동 확인하지 않는다.
- 자체 등록정보와 분류·인원 항목을 신규 업무에서 분리한다. 사진·계획은 보조 기록 도구이며 기존 자료·작성 업무는 보존한다.
- 미응답과 0명을 구분한다. 직접고용 중 기간제·단시간은 부분집합으로 검사하고 파견·다른 업체 인원을 법령별 인원으로 합산하지 않는다. 불명확한 업종·작업·인원·집계 범위는 추가 질문으로 남긴다.
- 업종·작업과 도급 관계로 관련 후보를 탐색하며 인원은 검토 순서에만 반영한다. 전체 열람은 76개 업무 후보를 제공한다. 연구 원장 78개와 기존 정의 81개는 이력·호환성을 위해 보존하며 전체 법정 의무 수로 표시하지 않는다.
- 프로필 revision과 이전/이후 감사 이력을 보존한다. 임시 저장은 미확인 상태이며 확인 전 신규 업무 생성은 서버와 DB에서도 차단한다. 기존 작성 기록은 정보 수정 중에도 열 수 있다. 기존 4인자 저장 RPC를 제거해 확인 우회를 막는다.
- 사용자 요청으로 Orca Run `run_7211cb26aad0`에 GPT-6 Astra medium 작업자 3개(DB·화면·테스트)를 배정했다. `current`가 다른 폴더를 가리켜 실제 프로젝트 절대 경로로 정정했으며, 작업자들은 그 폴더에 변경하지 않았음을 확인했다. 입력란에 남은 지시문의 제출 상태를 확인·복구한 뒤 작업을 진행했다.
- 세 작업자의 성공 보고를 확인하고 조정자가 통합했다. DB Task `task_e06fd6658c2f`, 화면 Task `task_cfe68e634b42`, 테스트 Task `task_ab22033f0e38` 모두 종료되었으며 세 작업자 터미널은 release 완료, 미정리 작업자는 0개다.

### 변경 파일

- 입력·저장: `src/domain/workplace-profile.ts`, `tasks.ts`, `workspace.ts`, `src/components/business-profile.tsx`, `src/server/task-actions.ts`, `demo-tasks.ts`, `src/adapters/workspace.ts`.
- 화면: `task-home.tsx`, `task-board.tsx`, `task-detail.tsx`, `task-forms.tsx`, `/app`·`/workspace` 홈, 회원 설정, 랜딩, `src/app/task-design.css`.
- DB: `supabase/migrations/202609150007_workplace_profile.sql`, `tests/integration/tasks.test.ts`.
- 검증: `tests/unit/profile-actions.test.ts`, `workplace-profile.test.ts`, `tests/e2e/profile.ts`, `business-profile.spec.ts`, 기존 tasks·legal-basis·demo E2E.
- 안내: `docs/03_UX.md`, `DESIGN.md`, `DECISIONS.md`, `P2_SETUP.md`, `PORTALS_TASKS.md`, `plans/2026-09-15-business-profile-design.md`.

### 실제 실행·테스트 결과

| 명령·검증 | 결과 |
|---|---|
| `npm.cmd test -- --maxWorkers=1` | 10개 파일, 108개 단위 테스트 통과 |
| `npm.cmd run test:db -- --testTimeout=120000` | 최종 DB 코드 2개 파일, 23개 통과(89.80초) |
| `npm.cmd run lint` | 통과. 이후 통합 수정한 TSX·테스트 4개 파일의 추가 ESLint도 통과 |
| `npm.cmd run build` | 최종 프로덕션 빌드·TypeScript 검사 통과 |
| `npm.cmd run test:e2e` | 17개 중 16개 통과, 입력 화면의 768px 사이드바 글자 대비 1개 발견(4.4분) |
| 대비 보정 후 `npm.cmd run test:e2e -- tests/e2e/business-profile.spec.ts:132` | 남은 1개 통과(39.3초). 네 입력 단계의 320/360/768/1440px 넘침·axe 모두 통과 |
| `npm.cmd run catalog:check` | 기존 후보 78개 원장·CSV·공개 파일 일치 |
| `node test-results/inspect-layout.mjs` | 최신 일반 미리보기에서 정보 입력→업무 목록, 펼친 추가 질문의 axe, 시험 API 404 확인 |
| `git -c core.safecrlf=false diff --check` | 공백 오류 없음 |

브라우저 17개 시나리오는 전체 실행과 수정 후 해당 검사 재실행으로 확인했다. 저장 실패 시 입력 유지, 임시 저장 재개, 0/미응답 구분, 확인 체크, 직접 경로 차단, 업종/작업 변경, 기존 기록 보존, 조문 링크와 기존 회원·검토자 권한·자료 흐름을 검증했다. `test-results/profile-step-*.png`, `profile-preview-mobile.png`, `profile-task-preview-desktop.png`에 화면을 캡처하고 모바일 및 데스크톱 배치를 확인했다.

최종 DB 기본 실행과 직렬 재실행은 기존 백업·디스크 재시작 테스트의 30초 제한으로 각각 22/23이었다. 제한을 120초로 늘린 실행에서 23/23을 확인했으며 테스트 기본 설정을 변경하거나 실패 이력을 삭제하지 않았다. 실제 Supabase의 다중 연결 동시성·Auth·Storage·파일 검사와 운영 복구는 여전히 미검증이다.

### 실행 방법·다음 작업

- 최신 확인 주소: **http://127.0.0.1:3016**. 가상 체험 → 사업장 정보 입력 → 입력 확인 → 관련 업무 목록. 일반 미리보기에는 운영자 시험 설정을 넣지 않는다. 재실행은 `npm.cmd run dev`.
- 실제 연결 시 migration 0007까지 적용해야 한다. 메모리 데모의 데이터는 서버 재시작 시 초기화될 수 있으며 실제 고객 자료를 넣지 않는다.
- 다음 단계: 업종·고용관계·법령별 인원 기준을 검토한 적용 규칙과 조문에 연결한다. 현재 목록은 관련 검토 후보이며 승인된 실제 법령 적용 규칙은 0개다. 임의의 선임 기준·교육 시간·법정 기한을 추가하지 않았다.
- 실제 외부 연결·공개 배포·커밋·푸시는 수행하지 않았다. 기존 `/onboarding` 체험 경로와 원장·이전 기록은 보존했다.

## 2026-09-15 · 항목별 근거 법령·준비 이유

### 구현한 사용자 흐름

- 회원 업무 목록의 각 행에 근거 요약을 표시한다. 항목을 열면 입력 전에 ‘근거 법령·준비 이유’를 기본 펼침으로 확인한다. 관리 시작 후와 기존 후보 열람 화면에서도 같은 안내를 사용한다.
- 법령명·조항, 활동 수행/기록·보존 의무, 서류와의 관계, 적용 전 확인할 조건, 공식 원문, 확인한 시행본·확인일을 구분했다.
- 검토 후보 78개와 자체 서식 3종 모두에 표시 영역을 제공한다. 5개 후보는 공식 조문 본문을 확인해 6개 조문/항 연결을 제공하며, 73개 후보는 기존 조사 위치·관련 법령과 ‘조문 확인 중’ 상태를 유지한다.
- 자체 서식의 직접 작성 근거는 미연결로 표시한다. 특정 양식·사진을 법정 작성 의무로 자동 취급하거나 관련 활동이 불필요하다고 판정하지 않는다.
- 현재 원문 확인 결과를 보여주는 읽기 전용 안내이며, 과거 판정·문서·권한·업무 상태를 바꾸지 않는다. 법령 원문 스냅샷 저장·개정 추적·근거 승인 도구는 이번 범위에 포함하지 않았다.

### 변경 파일

- `src/domain/legal-basis.ts`: 조문별 관계·출처·시행일·확인일과 미확인/자체 서식 상태.
- `src/components/legal-basis.tsx`, `task-detail.tsx`, `task-board.tsx`, `review-catalog.tsx`, `src/app/task-design.css`: 목록 요약·상세 영역·모바일 배치.
- `tests/unit/legal-basis.test.ts`, `tests/e2e/legal-basis.spec.ts`: 확인 상태 오인 방지, 활동/기록 분리, 모든 정의의 표시, 실제 화면·원문 링크·접근성 검사.
- `docs/LEGAL_PREPARATION.md`, `docs/DECISIONS.md`, `docs/plans/2026-09-15-legal-basis-design.md`: 확인한 조문 출처와 구현 결정.

### 실제 검증

| 명령·확인 | 결과 |
|---|---|
| `npm.cmd test` | 8개 파일, 98개 단위 테스트 통과 |
| `npm.cmd run lint` | 통과 |
| `npm.cmd run build` | 프로덕션 빌드·TypeScript 검사 통과 |
| `npm.cmd run catalog:check` | 기존 78개 후보·문서·CSV·공개 원본 일치 |
| `git -c core.safecrlf=false diff --check` | 공백 오류 없음 |
| `npm.cmd run test:e2e` | 13개 시나리오 모두 통과(3.4분). 새 근거 흐름 2개와 기존 작성·검토·권한·실패 복구 흐름 포함 |
| Edge·axe·반응형 | 새 근거 영역 320/360/768/1440px 가로 넘침 없음. 모바일 접근성 검사와 기존 후보 화면의 키보드·접근성 검사 통과 |

`test-results/legal-basis-desktop.png`, `legal-basis-mobile.png`에 화면을 캡처했다. 조문·의무 구분·원문 링크·확인 상태의 표시와 한글 줄바꿈을 확인했다. 캡처와 실행 도구는 Git 제외 경로이며 다음 브라우저 검증에서 갱신된다.

DB 스키마·권한 변경이 없어 DB 테스트는 이번에 재실행하지 않았다. 앞 단계의 로컬 DB 17개 통과 기록과 구분한다. 실제 법령 전수·현행본·예외·하위 규정 대조 및 외부 전문 검토는 미완료다. 교육 조문 검색 발췌와 본문 시행일 차이를 확인해 본문 표기와 추가 대조 상태를 함께 기록했다. 승인된 실행 규칙은 0개다.

### 실행 방법·다음 작업

- 확인 주소: **http://127.0.0.1:3016**. 가상 체험 → 업무 목록 → 항목 선택으로 근거를 확인한다. 다시 실행하려면 `npm.cmd run dev`를 사용한다.
- 다음 법령 작업: 미연결 73개 후보의 활동·서류별 조문을 대조하고, 확인한 5개 후보도 현행 시행본·예외·하위 규정과 사업장 적용 조건을 검토한다. 상세 출처는 `LEGAL_PREPARATION.md`에 보존했다.
- 실제 Supabase 인증·파일 검사·운영 복구 대기는 유지한다. 공개 배포·실제 고객 자료 처리·커밋·푸시는 하지 않았다.

## 2026-09-15 · 회원 업무 목록·중앙관제 분리·디자인 개편

이 절과 아래 기울기 제거·P2 기록은 당시의 결과로 보존한다. 최신 추가 작업은 문서 상단을 따른다.

### 구현한 사용자 흐름

- 공개 첫 화면 → 회원 로그인 또는 회원 전용 체험. 중앙관제는 별도 `/ops/login`으로 진입하며 회원 메뉴·공개 랜딩에 운영자 기능이나 역할 전환을 표시하지 않는다. 기존 역할 쿠키를 권한으로 사용하지 않는다.
- 회원 홈 → 분야별 업무·관련 검토 후보 열람 → 관리 시작 → 담당자·자료 준비 목표일·확인 내용 저장 → 증빙·내용 확인 → 검토 요청 → 별도 검토자 의견 → 새 버전 보완·재검토. 선택한 검색·상태와 목록 복귀 위치를 보존한다.
- 검토 후보 78개에 공통 기록, 등록정보·사진 점검·계획 자체 서식 3종에 전용 입력란을 제공한다. 총 81개 정의는 법정 의무 수가 아니며 미확인 값은 비워둔다. 문서·수행·사람 검토·접수 상태는 별도다.
- 자료 준비 목표일 경과는 빨강, 7일 이내는 주황과 상태 문구로 강조한다. 한국 날짜를 사용하며 검토 중·자료 검토 완료 건은 준비 기한 강조에서 제외한다.
- 실제 환경은 조직·현장별 업무와 기존 문서 이력을 연결한다. 같은 현장·민감도인 미연결 문서만 연결할 수 있다. 자료함에서 연결된 문서를 열면 전용 업무 양식으로 이동한다.
- 운영자 등록은 회원과 별도 계정으로 관리한다. 서버와 DB에서 등록·추가 인증·배정·민감자료 권한을 확인하고 철회·동시 수정·다른 고객 접근을 거부한다. 법령 관리자에게 고객 검토 권한을 자동 부여하지 않는다.
- 원본의 크림·코랄·세리프 제목을 바탕으로 수평 두 열 랜딩, 행 중심 업무 목록과 기록 화면을 적용했다. 장식용 기울기는 없다. 새 외부 폰트를 설치하지 않았으며 시스템 한글 명조 대체 글꼴을 사용한다.

### 주요 변경 파일

- `src/domain/tasks.ts`, `src/server/task-actions.ts`, `src/server/demo-tasks.ts`, `src/adapters/workspace.ts`: 업무 정의·저장·권한·문서 연결.
- `supabase/migrations/202609150004_tasks_portals.sql`~`202609150006_task_write_access.sql`: 운영자 등록, 업무·조건·후보 메타데이터, RLS·감사·동시 수정 검사.
- `src/server/supabase.ts`, `src/server/operator-actions.ts`, `src/server/store.ts`, `src/server/workspace-actions.ts`, `src/proxy.ts`: 회원/운영자 인증 경로·액션 보호.
- `src/components/task-*.tsx`, `document-panel.tsx`, `operator-*.tsx`, 관련 `/app`·`/workspace`·`/ops` 경로: 목록·전용 기록·자료함·중앙관제.
- `src/app/page.tsx`, `src/app/task-design.css`, `src/components/navigation.tsx`, `src/components/shell.tsx`, `docs/DESIGN.md`: 디자인·탐색 개편.
- `tests/unit/tasks.test.ts`, `task-repository.test.ts`, `tests/integration/tasks.test.ts`, `tests/e2e/tasks.spec.ts`, `actors.ts`, `scripts/check-browser.mjs`: 업무·권한·분리 세션 검증. 기존 검증 시나리오를 유지했다.
- `docs/PORTALS_TASKS.md`, `P2_SETUP.md`, `03_UX.md`, `DECISIONS.md`, `README.md`, 실행 계획: 현재 연결·사용·검증 절차.

### 실제 검증

| 명령·확인 | 결과 |
|---|---|
| `npm.cmd test` | 7개 파일, 94개 단위 테스트 통과. 목표일·빈값·페이로드, 실제 SDK의 HTTP 요청 계약 포함 |
| `npm.cmd run test:db` | 2개 파일, 17개 로컬 PostgreSQL 테스트 통과. RLS·중복·버전·감사·민감도 연결·운영자 등록/철회·MFA·복원 포함 |
| `npm.cmd run lint` | 통과 |
| `npm.cmd run build` | 프로덕션 빌드·TypeScript 검사 통과. 운영자 경로는 동적 렌더링 |
| `npm.cmd run catalog:check` | 78개 후보·문서·CSV·공개 원본 일치 |
| `git -c core.safecrlf=false diff --check` | 공백 오류 없음 |
| `npm.cmd run test:e2e` | 11개 시나리오 모두 통과(2.9분). 기존 흐름·후보 목록·핵심 양식 3종·공통 기록·독립 검토 세션·보완 재제출·타 고객/역할 변조 차단·실패 입력 보존 포함 |
| Edge·axe·반응형 검사 | 최종 랜딩·업무 목록·양식 접근성 검사 통과. 목록 320/360/768/1440px 가로 넘침 없음. 기울기 없음·검색 복원·키보드·확대 상당 폭의 기존 검사도 통과 |
| 일반 서버 HTTP 확인 | `/`, `/login`, `/ops/login`, `/emergency` 200. 비인증 `/ops`→`/ops/login`, `/workspace`→`/login` 307. 시험 계정 생성 POST는 404 |

최종 화면은 `test-results/landing-1440.png`, `landing-360.png`, `task-list-desktop.png`, `task-list-mobile.png`, `task-member-detail.png`, `task-operator-review.png`에 캡처했다. 육안으로 한글 줄바꿈·수평 정렬·모바일 탐색과 양식 배치를 확인했다. 캡처 검토에서 추가로 확인한 검토 상세의 자료 제목 누락과 의견 없는 완료 건의 대기 문구를 수정하고 브라우저 검증에 포함했다. 이 경로는 Git 제외이며 다음 E2E 실행 시 다시 생성된다. 실제 현장 사용자·실물 모바일 기기 검증과 구분한다.

초기 재검토 검사는 선택 상자의 레이블 탐색이 일치하지 않아 시간 제한에 도달했다. 실제 Edge에서 레이블·접근성 역할 조회를 비교해 원인을 재현하고 `combobox` 역할로 선택자를 수정했다. 임시로 늘렸던 제한은 기존 90초로 되돌렸다. 추가 검사에서 발견한 랜딩 보조 면의 링크 대비(4.18:1)는 짙은 코랄로 보정했고, 폼 오류와 Next.js 경로 안내를 구분하도록 오류 선택자를 수정했다. 별도 브라우저 세션도 파일마다 정리한다. DB 복원 검사는 빌드와 동시 실행 시 1회 시간 제한에 도달했지만 순차 재실행에서는 17개 모두 통과했다.

### 실행 방법·미연결·다음 작업

- 일반 확인 주소: **http://127.0.0.1:3016**. 시험 전용 권한 설정을 제거한 데모 서버다. 새 실행은 `npm.cmd run dev` 또는 빌드 후 `npm.cmd run start -- --port 3016`을 사용한다.
- 중앙관제 진입 주소는 `/ops/login`이다. 실제 인증 설정이 없는 현재 환경에서는 연결 준비 상태를 표시한다. 일반 확인 서버에는 운영자 체험으로 전환하는 기능을 두지 않는다.
- 브라우저 검증은 기본 3015번의 별도 서버와 임의 토큰으로 분리 계정을 만든다. `/api/testing/session`은 정상 데모 설정에서 404이고 live에서는 항상 차단한다.
- 실제 Supabase Auth/MFA/PostgREST/Storage, 파일 검사 공급자·워커, DB+실제 원본 파일 복구는 미연결·미검증이다. 로컬 DB 및 HTTP 모의 응답 검증을 외부 서비스 검증으로 보고하지 않는다.
- 다음 작업은 비공개 시험 환경에 마이그레이션·별도 운영 계정·배정을 적용하고 실제 두 계정의 업로드→검사→검토→재제출과 권한 철회·복구를 확인하는 것이다. 연결 절차는 `PORTALS_TASKS.md`와 `P2_SETUP.md`에 정리했다.
- 실제 법령 승인 규칙 0개, 현장 담당자 사용성 검증, AI/OCR·PDF 출력은 남아 있다. 공개 배포·고객 초대/발송·유료 구매·커밋·푸시는 실행하지 않았다. 기존 미커밋 작업을 보존했다.

## 2026-09-15 · 랜딩 기울기 제거·디자인 개정안·후속 계획

### 구현과 문서 변경

- `src/app/globals.css`: 랜딩 업무 카드의 `transform:rotate(-1deg)`만 제거했다. 기존 미커밋 변경을 보존했다.
- `docs/DESIGN.md`: 제공 원본과의 우선순위, 세 가지 방향의 장단점, 추천 B안의 색상·글꼴·간격·소개/업무 화면 구분, 장식·상태 규칙과 검수 절차를 작성했다. P1 당시 기준은 부록에 보존했다.
- `docs/DECISIONS.md`: 최신 사용자 지시와 원본 준수 원칙, 이번에 적용한 수정과 제안 상태를 기록했다.
- `docs/plans/2026-09-15-design-p2-next.md`: 디자인 방향 선택 → 대표 화면 → 전체 확장, P2 실제 연결·검사·복구 → P3 법령 원장 순서와 완료 기준을 작성했다.
- 전체 색상·글꼴·배치는 아직 개편하지 않았다. 기업 브랜드와 업무 가독성의 균형을 추천안으로 작성했으며 사용자 확정으로 기록하지 않았다.

### 실제 검증

| 명령·확인 | 결과 |
|---|---|
| `npm.cmd run build` | 프로덕션 빌드·빌드 과정 TypeScript 검사 통과 |
| `npm.cmd run start -- --port 3014` | 이번 빌드의 확인용 서버 시작 |
| `node test-results/verify-landing.mjs` | 실제 Edge에서 1440/360/320px 모두 transform=none, 가로 넘침 없음, 카드가 화면 안에 위치. 체험 범위 링크 `/demo` 이동 성공, pageerror 0건 |
| 스크린샷 육안 확인 | `test-results/landing-upright-1440.png`, `landing-upright-360.png`에서 카드 수평·텍스트 배치 확인. 320px 캡처도 저장 |
| sRGB 대비 계산 | 원본 코랄/흰색 약 3.277:1, 코랄/짙은 글자 약 5.467:1, 짙은 코랄/흰색 약 5.057:1. 개정안의 후보 조합이며 실제 새 색상 적용 검증과 구분 |
| `git -c core.safecrlf=false diff --check` | 공백 오류 없음 |

일회성 브라우저 확인 스크립트·결과 JSON·이미지는 Git 제외 경로인 `test-results/`에 보관했다. 이번 CSS 수정에는 새 회귀 테스트를 추가하지 않았다. 전체 단위·DB·E2E·axe 검사와 실제 기기 검증은 이번에 재실행하지 않았으며 이전 P2 검증 결과와 구분한다.

### 실행 방법·다음 작업·미완료

- 확인 주소: **http://127.0.0.1:3014**. 새로 실행하려면 `npm.cmd run dev` 후 표시된 로컬 주소를 연다.
- 다음 디자인 작업: B안 또는 사용자 선택안을 기준으로 랜딩·업무 목록·자료 상세/검토의 대표 화면을 구현하고 모바일·데스크톱에서 확인한 뒤 나머지 화면에 확장한다.
- 다음 기능 작업: 시험용 Supabase 프로젝트를 비공개 설정으로 연결해 실제 Auth/MFA/Storage·권한을 검증한다. 파일 검사 공급자·워커와 DB+원본 복구, 운영 권한 관리·계정 복구 화면은 여전히 남아 있다.
- 외부 연결 대기 중에는 키 없이 진행할 수 있는 디자인·운영 화면·P3 원장 구조 준비가 가능하다. 실제 법령 승인 규칙 0개와 현장 사용자 검증 대기는 유지한다.
- 공개 배포·실제 고객 초대/발송·유료 구매는 실행하지 않았다. 코드·문서는 기존 작업과 함께 로컬 미커밋 상태다.

## 2026-09-15 · P2 인증·저장 기반

### 구현한 흐름

- 기존 P0·P1과 검토 목록의 미커밋 변경을 보존했다. 실제 데이터용 `/login`·`/workspace`를 추가하고 live에서 데모 서버 액션을 차단한다.
- 기존 Supabase 계정 로그인 → 새 빈 조직·첫 현장 또는 이메일에 묶인 초대 수락 → 자료 초안 작성 → 내용 확인 → 검토 요청 → 배정 검토자의 보완 → 새 버전 작성 → 자료 검토 흐름을 연결했다.
- 인증 앱 등록·AAL2 검증을 추가했다. 검토자 접근은 배정 현장·기간·철회 여부·업무 범위와 추가 인증을 함께 확인한다. 민감자료는 별도 권한이다.
- 조직·현장·문서·버전의 복합 외래키, RLS와 최소 권한을 적용했다. 일반 클라이언트는 직접 테이블을 수정·삭제하거나 원문을 SELECT할 수 없다. 원문 읽기는 감사 RPC로 수행한다.
- 문서·보완 의견·수행 정정·권한 변경의 이전/이후 이력을 보존한다. 중복 검토 요청은 기존 요청을 반환하며 오래된 revision의 문서·수행 변경은 거부한다. 기관 접수는 미확인으로 유지한다.
- 초대 코드는 이메일·현장·쓰기 권한에 묶이고 48시간 후 만료된다. DB에는 해시만 저장하며 수락·철회·재사용·철회된 계정 재활성화를 검증한다. 실제 이메일 발송은 하지 않는다.
- 파일은 10MiB 제한·파일명·MIME·확장자·시작 바이트를 사전 확인하고 비공개 격리에 저장한다. 검사 공급자 미연동 시 clean으로 처리하지 않는다. 다운로드는 현재 권한·감사·검사 상태·원본 해시를 확인한다. 직접 서명 URL 발급·원본 덮어쓰기는 차단한다.

### 변경 파일

| 범위 | 파일 |
|---|---|
| DB·권한·트랜잭션 | supabase/migrations/202609150001_p2_core.sql, 202609150002_p2_files.sql, 202609150003_invitation_list.sql |
| 인증·서버 동작 | src/server/supabase.ts, workspace-actions.ts, src/proxy.ts |
| 연결부·검증 계약 | src/adapters/workspace.ts, evidence.ts, src/domain/workspace.ts, files.ts |
| 회원 공간·파일 API | src/app/login/**, src/app/workspace/**, src/app/api/files/** |
| 폼·첨부·인증 화면 | src/components/workspace-form.tsx, mfa-forms.tsx, evidence-upload.tsx |
| 모드·기존 경로 | src/domain/config.ts, src/server/store.ts, actions.ts, src/app/page.tsx, demo/page.tsx, globals.css |
| 테스트 | tests/unit/p2-adapters.test.ts, tests/integration/**, tests/e2e/p2-boundaries.spec.ts, demo.spec.ts, vitest.db.config.ts, playwright.config.ts |
| 설정·안내 | package.json, package-lock.json, .env.example, scripts/setup-storage.mjs, README.md, docs/P2_SETUP.md, DECISIONS.md, plans/2026-09-15-p2-auth-storage.md |

### 실제 실행·검증

| 명령·검증 | 결과 |
|---|---|
| npm 공식 버전 조회·설치 | 샌드박스 EACCES 후 승인된 외부 실행. Supabase SSR 0.12.7·SDK 2.116.0·PGlite 0.5.8 고정. 설치 감사 출력 취약점 0개 |
| docker info | Docker Desktop 실행 엔진이 꺼져 있어 연결 불가. PGlite 로컬 SQL 검증으로 진행 |
| npm.cmd test | P2 연결·파일·모드 경계와 기존 단위 테스트 통과. 최종 건수는 아래 최종 확인 기록에 기재 |
| npm.cmd run test:db | 10개 통과. 실제 PostgreSQL SQL·RLS·복합 FK·원본/검토·초대·민감자료·Storage 정책·디스크 복원/종료/재시작 확인 |
| npm.cmd run typecheck | 통과 |
| npm.cmd run lint | 초대 만료 계산·파일명 제어문자 검사·테스트 finally 정리에서 발견한 오류를 수정 후 통과 |
| npm.cmd run catalog:check | 기존 78개 검토 목록의 문서·CSV·공개 다운로드 원본 일치 |
| npm.cmd run build | 프로덕션 빌드 통과. 실제 작업 공간·파일 API·인증 proxy 포함 |
| 첫 npm.cmd run test:e2e | 기존 6개 통과, 신규 1개 실패. 요청 출처를 내부 URL과 비교해 동일 출처 업로드도 403으로 거부한 문제 수정. 미연결 공간의 병렬 렌더링에서 인증 오류 로그가 남는 문제도 수정 |
| 첫 E2E 종료 | Windows 샌드박스에서 테스트 서버 자동 종료가 지연됨. 승인된 프로세스 확인으로 이번 작업의 3012 Next.js 서버만 종료. 기존 3000·3001 서버는 종료하지 않음 |
| 후속 브라우저 검증 | 별도 실행한 확인용 서버를 사용해 재검증. 최종 결과는 아래 기록 |

### 데모·미연동·미검증 범위

- 현재 실제 Supabase 설정 파일·계정 연결이 없다. Auth/MFA·쿠키 갱신·PostgREST·Storage 서비스의 실제 통합 검증은 미실행이다. 연결부 단위 테스트는 실제 SDK에 시험 HTTP 응답을 제공하며, DB 테스트의 Auth/JWT·Storage 메타데이터는 시험 대역이다.
- PGlite SQL·디스크 재시작·백업 복원은 로컬 DB 검증이다. 실제 Supabase DB와 원본 파일을 함께 백업·복구하는 운영 검증은 남아 있다.
- 파일의 악성 여부·전체 구조·PDF 암호화·페이지 수를 검사할 공급자와 워커는 미연동이다. `FileInspector` 계약과 미연동 반환을 제공하며 파일은 격리 상태를 유지한다.
- 검토자 배정·민감자료 권한 부여·기존 구성원 접근 철회는 DB 운영 절차로 제공한다. 이 운영 화면, 계정 자체 가입·복구 화면, 실제 초대 발송은 후속 작업이다.
- 실제 법령 승인 규칙 0개, 현장 담당자 사용성·실제 기기 검증, P3 승인·개정 영향, P4 실제 AI/OCR/PDF, P5 운영·비용·보존/삭제·보안 검토는 미완료로 유지한다.
- Orca 런타임·독립 에이전트 교차 검토는 재시도하지 않았다. 이번 작업은 단일 에이전트로 수행했다.
- 실제 고객 자료·외부 발송·공개 배포·유료 자원 구매는 실행하지 않았다. 코드와 기존 변경은 로컬 미커밋 상태다.

### 실행 방법과 다음 작업

1. 키 없이 기존 데모: `npm.cmd run dev`, 루트 화면 → 가상 사업장 체험. `/login`은 실제 연결 준비 상태를 안내한다.
2. 로컬 DB 검증: `npm.cmd run test:db`. 실제 설정·마이그레이션·비공개 버킷·검토자 배정은 `docs/P2_SETUP.md`를 따른다.
3. 다음 외부 작업: 시험용 Supabase 프로젝트 연결 후 실제 로그인·MFA·다른 고객/현장 차단·원본 Storage 호출을 검증한다. 키는 비공개 설정에만 둔다.
4. 파일 검사 공급자 연결 및 DB+원본 파일 복구를 검증한다. 운영 권한 관리 화면·계정 복구를 이어서 구현한다.
5. 시설관리·유지보수 검토 후보의 사용자 1차 검토·공식 원문 대조·외부 전문 검토는 별도 작업으로 유지한다.

### 최종 확인 기록

- `npm.cmd test`: 최종 5개 파일·86개 통과. 기존 62개 + P2 설정·파일·HTTP 연결 계약·요청 출처 24개.
- `npm.cmd run test:db`: 10개 통과 (43.71초). PostgreSQL 정책·트랜잭션·초대/권한 감사·Storage 작업 제한·디스크 복원과 DB 재시작 확인.
- 최종 `npm.cmd run build`·타입 검사·`npm.cmd run lint` 통과. 기존 검토 목록 `catalog:check` 일치. `git diff --check` 공백 오류 없음.
- `npm.cmd run start -- --port 3013`으로 확인용 프로덕션 서버 실행. `PLAYWRIGHT_PORT=3013`, `CI` 비설정으로 이 서버를 사용한 `npm.cmd run test:e2e`: **7개 전체 통과 (49.8초)**. 마지막 `.last-run.json`은 passed, 실패 목록 없음.
- 신규 로그인 안내 360px 화면: 가로 넘침 없음, axe 선택 WCAG 규칙 위반 0건, `test-results/p2-login-mobile.png` 육안 확인. 실제 로그인 뒤 화면과 MFA·업로드의 공급자 통합 브라우저 검증은 설정이 없어 미실행.
- 확인용 주소: **http://127.0.0.1:3013**. `/login` HTTP 200 재확인. 회원 공간은 현재 연결 준비 안내를 표시하며, 기존 가상 사업장 체험을 계속 사용할 수 있다.

---

아래는 P2 시작 전의 P0·P1 작업 당시 기록이다.

## 2026-09-15 · 이번 요청에서 구현한 흐름

- 기존 저장소·Git 상태·잠금 파일·Node/npm·명령을 확인했다. 작업 시작 시 변경 없음. Node 24.13.0, npm 11.6.2. 기존 의존성 버전을 유지했다.
- 회원: 가상 사업장 시작 → 더보기 → 업종·인원별 검토 목록. 운영자: JH 배정 검토자 → 법령 준비.
- 18개 업무별 업종 묶음·10개 인원 선택지·8개 분야·78개 검토 후보를 제공한다. 공식 산업분류 코드나 전체 법정 의무 수가 아니다.
- 업종·분야·검색어로 탐색하고 실제 작업·설비 조건을 추가하면 업종 밖의 관련 후보도 포함한다. 인원은 우선 정렬·추가 확인 안내에만 사용하며 후보를 자동 제외하지 않는다.
- 각 항목의 적용 확인 조건·질문·증빙 예시·조사 위치·공식 출처·확인 범위를 표시한다. 모든 항목은 적용 여부·전문 검토 대기이며 실행 규칙으로 사용하지 않는다.
- 현재 필터 결과 CSV 다운로드, 전체 Markdown 다운로드와 로컬 검토용 CSV를 제공한다. 검토자·검토일·의견은 파일 사본에 기록하고 서버에 저장하지 않는다.
- 성희롱 예방·장애인 인식개선·퇴직연금·개인정보 교육은 안전보건교육과 구분한 연관 검토 항목이다. 전체 직원에게 같은 시간·주기를 자동 적용하지 않는다.

### 변경 파일

| 파일 | 역할 |
|---|---|
| src/domain/review-catalog.ts | 후보·업종·인원·작업·출처 원본, 필터·정렬·CSV |
| src/components/review-catalog.tsx | 공통 검색·상세·출처·다운로드 화면 |
| src/app/app/checklists/page.tsx, src/app/ops/rules/page.tsx | 회원·운영자 검토 목록 경로 |
| src/app/app/settings/page.tsx, src/app/globals.css | 회원 진입 링크, 기존 디자인에 맞춘 반응형 목록 |
| scripts/export-review-catalog.mjs, package.json | 원본에서 문서·CSV 생성 및 일치 확인 명령 |
| docs/CHECKLIST_REVIEW.md, docs/CHECKLIST_REVIEW.csv, public/review-catalog.md | 사용자 검토용 산출물·다운로드 |
| tests/unit/review-catalog.test.ts, tests/e2e/catalog.spec.ts | 인원·범위·CSV·실제 브라우저 흐름 검증 |
| README.md, docs/DECISIONS.md, docs/LEGAL_PREPARATION.md, docs/plans/2026-09-15-p0-p1-review-catalog.md | 실행·결정·법령 확인 범위·구현 계획 |

### 실제 실행·검증

| 명령·검증 | 결과 |
|---|---|
| node --version / npm.cmd --version | v24.13.0 / 11.6.2 |
| npm.cmd run catalog:export | 78개 후보의 Markdown·CSV·공개 정적 다운로드 생성 |
| npm.cmd run catalog:check | 3개 산출물과 원본 일치 |
| npm.cmd run typecheck | 통과 |
| npm.cmd run lint | 최초 생성 스크립트의 URL/process 전역 선언 오류 6개 발견. Node 내장 모듈 명시 import 후 통과 |
| npm.cmd test | 4개 파일·62개 테스트 통과. 기존 30개와 신규 32개 |
| npm.cmd run build | 통과. 회원 /app/checklists·운영자 /ops/rules 포함 16개 경로 출력 |
| PowerShell Import-Csv | 78행·21열, ID 중복 없음, 전 행 검토 대기, 검토자 빈칸 확인 |
| npm.cmd run test:e2e | 기존 P1 4개 통과. 신규 목록 2개는 select의 label 문자열을 찾는 테스트 선택자 문제로 실패 |
| npm.cmd run test:e2e -- tests/e2e/catalog.spec.ts | 선택자를 접근성 역할 combobox+이름으로 수정 후 신규 2개 통과 (28.6초). 같은 빌드에서 기존 4개+신규 2개 검증 완료 |
| 화면·접근성 확인 | 목록 360px 키보드 상세 열기·검색·초기화·axe 선택 WCAG 규칙 위반 0건. 320/360px 가로 넘침 없음. 데스크톱·모바일 상세 캡처 육안 확인 |
| npm.cmd run start -- --port 3001 | 확인용 프로덕션 서버 http://127.0.0.1:3001 실행. 키 없이 가상 사업장 체험 가능 |
| 확인용 서버 HTTP 요청 | /, /emergency, /review-catalog.md 모두 HTTP 200 확인 |
| git diff --check | 공백 오류 없음. Windows 줄바꿈 안내만 출력 |

### 법령·실제 연결·남은 작업

- 법령: 국가법령정보센터·고용노동부·공단 자료의 발췌·서지 확인과 원문 조사 경로를 구분했다. 산안법 시행령·시행규칙의 2026-08-01 시행본 서지는 확인했으나 별표·예외·경과조치 전체 대조는 미완료다. 출처별 확인 상태·실제 확인 URL은 검토 문서에 기록했다.
- 승인된 실제 규칙 0개. 교육시간·선임 수·법정 주기·제출 기한·보존기간은 목록에서 확정하지 않았다. 원문 및 사용자 1차 검토·외부 전문 검토·기대 사례 승인이 다음 작업이다.
- P2 실제 인증·조직/현장 권한·DB/RLS·원본 파일 저장·검사·백업·복구는 미구현이다. 현재 접근 제어는 서버 데모의 고객·배정 범위 시험이다.
- P4 실제 OCR·AI·PDF 및 외부 발송·기관 제출은 미연동이다. 생성되는 CSV/Markdown은 검토 목록 다운로드이며 법정 서식 출력·접수를 대신하지 않는다.
- 현장 담당자 참여 사용성 검증·실제 모바일 기기·보조기기 검증은 미실행이다.
- Orca 런타임·독립 에이전트 교차 검토는 이번에 재시도하지 않았다. 단일 에이전트 작업이며 이전 미완료 항목을 유지한다.
- 공개 배포·외부 발송·유료 자원 구매 없음. 실행 방법은 README의 로컬 데모 안내를 따른다.

### 바로 확인하기

1. 현재 확인용 서버: http://127.0.0.1:3001 → 가상 사업장으로 체험하기.
2. 더보기 → 업종·인원별 검토 목록. JH 역할은 법령 준비 메뉴.
3. 음식점·급식과 0명을 선택해 공통 후보가 유지되는지 확인하고, 실제 작업의 차량·하역을 추가해 지게차 항목을 열어본다.
4. 현재 목록 CSV를 내려받아 사본에 검토자·검토일·의견을 기록한다. 전체 자료는 docs/CHECKLIST_REVIEW.md 및 docs/CHECKLIST_REVIEW.csv.
5. 다음 작업은 시설관리·유지보수 후보의 현장 사실·원문·예외·시행일을 사용자 1차 검토로 대조하고 필요한 전문 검토를 연결하는 것이다. P2 실제 인증·저장 작업은 별도 단계로 유지한다.

---

## 2026-09-14 기록 (당시 결과 보존)

당시 단계: P0 기반·P1 핵심 데모 구현 및 기술 검증 완료, 사용자 검증 대기

## 확인한 환경
- Markdown 10개, 기존 코드·Git 없음. 기존 문서를 보존하여 Git main 초기화.
- Node 24.13.0, npm 11.6.2. npm.cmd 실행 가능.
- npm 네트워크 조회는 샌드박스 EACCES 후 승인된 외부 실행으로 확인.
- Orca 설치 CLI를 발견했으나 PATH/Path 중복 오류로 시작 실패. 중복 환경 키를 정리한 실행도 20초 시간 초과. 실제 task/dispatch 생성 없이 단일 에이전트로 구현했다.

## 구현한 사용자 흐름
- 가상 사업장 시작 → 3단계 정보 입력 → OCR 샘플 후보 확인·수정 → 임시 저장·사용자 확인. 0명과 미응답, 개업일과 발급일을 구분한다.
- 회원 홈 → 준비할 업무 → 가상 사진 첨부/자체 서식 초안 → 내용 확인 → 검토 요청.
- 배정 검토자의 자료 확인 → 위치가 명시된 보완 의견 → 회원의 새 버전 작성·재제출 → 자료 검토 완료. 원래 버전·의견·수행 기록의 정정 이력을 보존한다.
- 문서 작성·수행 기록·사람 검토·외부 접수 상태를 분리한다. 검토 완료가 법적 이행이나 기관 접수로 바뀌지 않는다.
- 다른 고객·미배정 검토자의 자원 조회·변경을 서버 데모 범위에서 차단한다. 회원 응답에 운영자 내부 메모를 포함하지 않는다.
- 전송 실패 시 작성 내용을 유지하고 재시도한다. 공개 `/emergency`는 로그인·체험 세션·AI 없이 접근한다.

## 변경 파일
| 범위 | 파일 및 역할 |
|---|---|
| 실행 기반 | package.json, package-lock.json, tsconfig.json, next-env.d.ts, next.config.ts, postcss.config.mjs, eslint.config.mjs, .gitignore, .env.example |
| 도메인·데모 | src/domain/{types,rules,workflow,config}.ts, src/demo/seed.ts: 사실 스냅샷·3값 논리·버전별 검토·충돌 방지·가상 사업장 |
| 서버·연결부 | src/server/{store,actions}.ts, src/adapters/registration.ts: 세션별 메모리 상태·서버 검증·OCR 후보 인터페이스 |
| UI | src/app/**, src/components/**: 회원·운영자·정보 입력·문서 버전·긴급 화면, 크림/파랑 반응형 디자인 |
| 검증 | vitest.config.ts, playwright.config.ts, tests/unit/{rules,workflow,adapters}.test.ts, tests/e2e/demo.spec.ts |
| 기록 | README.md, docs/{DECISIONS,DESIGN,LEGAL_PREPARATION,ORCHESTRATION,PROGRESS}.md |

기존 기획 Markdown은 보존했다. README에는 실행 안내를 추가하고 원본 내용을 유지했다.

## 실제 실행 및 결과
| 명령/검증 | 결과 |
|---|---|
| npm.cmd install | 잠금 파일 생성·버전 고정. 최종 설치 출력: 231개 패키지 감사, 취약점 0개 보고. 전체 보안 감사의 의미는 아님 |
| npm.cmd run typecheck | 통과. 초기 메모리 Map 타입 추론 오류 수정 후 확인 |
| npm.cmd run lint | 최종 ESLint 10·TypeScript·Hooks·Next 플러그인 구성에서 통과 |
| npm.cmd test | 3개 파일, 30개 테스트 통과: 미확인/0 구분, 합성 경계, 판정 재현, demo/live 차단, 버전·검토·권한·동시 수정, OCR 후보, 수행 이력 |
| npm.cmd run build | Next.js 프로덕션 빌드 통과. 공개 긴급 화면 및 동적 체험·회원·운영자 경로 생성 |
| npm.cmd run test:e2e | Microsoft Edge, 4개 시나리오 모두 통과 (50.4초). 이전 화면 이동 대기 오류를 고친 뒤 전체 재실행 |
| 접근성·화면 | 홈 360px에서 axe WCAG 2 A/AA·2.1 AA·2.2 AA 선택 규칙 위반 0건. 320/360px·720 CSS px 확대 상당 환경에서 가로 넘침 없음. 공개 긴급 경로의 키보드 119 접근 확인 |
| 화면 육안 확인 | 데스크톱·모바일 캡처 확인. 전체 페이지 캡처의 스크롤 위치 영향을 별도 Edge 뷰포트로 확인: 본문 건너뛰기 링크는 평상시 화면 밖, 사이드바 시작 위치 정상 |
| npm.cmd ls --depth=0 | 필수 패키지 버전 확인. 로컬에 sharp의 선택적 wasm 패키지 1개가 extraneous로 표시됨; node_modules는 커밋 대상에서 제외 |
| git diff --check | 공백 오류 없음. Windows 줄바꿈 변환 안내만 출력 |
| 비밀 설정 제외 | git check-ignore로 .env.local/.env.production, node_modules, .next, test-results, playwright-report 제외 확인 |

E2E 세부: ① 제출→보완→새 버전 재제출→검토, 내부 메모·다른 고객·미배정 접근 차단 ② 입력·임시 저장·확인·미응답 ③ 모바일·접근성·공개 긴급 경로 ④ 전송 실패·입력 보존·재시도.

초기 axe 검사에서 장식 숫자 대비 부족을 발견해 수정했다. 테스트가 검토 상세 이동 전에 이전 URL을 저장하던 문제는 상세 URL 도착을 기다리도록 수정했다. Orca agent-browser는 연결 시간 초과로 사용하지 못해 Playwright에서 설치된 Edge를 직접 실행했다. 실패를 성공으로 집계하지 않았다.

화면/테스트 산출물은 로컬 test-results/ 및 playwright-report/에 있으며 Git에서는 제외한다. 200% 상당 레이아웃 검증은 실제 브라우저 확대 조작 시험과 구분한다.

## 실행 방법
1. Node 24·npm 11 환경에서 `npm.cmd ci`, `npm.cmd run dev`.
2. http://127.0.0.1:3000 → 가상 사업장으로 체험하기.
3. 역할 메뉴에서 A 고객 → JH 배정 검토자 → A 고객으로 전환하여 보완 흐름 확인.
4. 브라우저 자동 검증은 다른 3000번 포트 서버를 종료하고 `npm.cmd run build`, `npm.cmd run test:e2e` 순서로 실행.

키 없이 실행한다. 메모리 상태는 같은 체험의 새로고침에는 유지되고 서버 재시작·초기화·세션 만료 시 사라진다. 실제 자료는 입력하지 않는다.

## 데모·미연동·미검증 범위
- P2 실제 인증·조직 소속 확인·DB/RLS·파일 저장·백업·복구·삭제 정책 미구현. 역할 전환은 가상 권한 체험이며 운영 인증이 아니다.
- 승인된 실제 법령 규칙은 0개. P3 법령 원문 조사·전문 검토·규칙 승인/배포·개정 영향 분석 미완료. DEMO_ 규칙은 실제 적용 의무가 아니다.
- P4 실제 OCR/AI·고객 파일·PDF 내보내기·전자서명·발송·기관 제출/접수 미연동. OCR는 제공된 샘플 후보만 반환한다. APP_MODE=live 및 AI/OCR live는 명시적으로 거부한다.
- 시설관리 담당자 참여 사용성 검증, 실제 iOS/Android·보조기기·브라우저 확대 조작 검증 미실행. axe 홈 검사를 전체 서비스 접근성 인증으로 해석하지 않는다.
- Orca 런타임 연결 및 에이전트별 독립 교차 검토 미완료. 후속 구성은 총괄 1명 + 작업자 최대 3명이며 docs/ORCHESTRATION.md에 배정/통합 기준을 기록했다.
- 공개 배포·외부 발송·유료 자원 구매 없음.

## 남은 차단 요인과 다음 작업
1. 시설관리 담당자에게 정보 확인·자료 준비·보완 재제출을 수행하게 하여 다음 행동과 상태 의미를 이해하는지 확인한다.
2. P2 실제 인증·영속 저장·조직/현장 권한을 연결하고 고객 간 격리·서버 재시작 복구를 검증한다. 관련 외부 계정/프로젝트 설정 필요.
3. P3 시설관리·유지보수 범위의 법령 원문과 적용 사실을 조사하고 사용자 1차 검토·외부 전문 검토를 거쳐 규칙을 승인한다.
4. Orca 연결 복구 후 독립 작업만 분리해 배정하고, 완료 기록과 통합 검증을 확인한다.
